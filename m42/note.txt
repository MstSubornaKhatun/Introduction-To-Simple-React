===============================
Module 42: Simple React SPA with Scribbles Café
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React SPA (Single Page Application) কীভাবে তৈরি হয়?
- Component, props, state, event handler এর ব্যবহার কীভাবে হয়?
- HW2 সিস্টেম (How, What, Why) কেন গুরুত্বপূর্ণ?
- এই মডিউল থেকে কী শেখা যাবে?

📝 Notes (বিস্তারিত তথ্য)

1. Module Overview
📌 Explanation:
- Module 42 → Simple React SPA বানানো হবে।
- Scribbles Café → একটি সিম্পল ব্লগিং সাইটের মতো main page তৈরি করা হবে।
- Features:
  - অনেকগুলো blog থাকবে।
  - Reading time দেখানো হবে।
  - Bookmark list এ যোগ করা যাবে।
  - Mark as read করা যাবে।
  - কিছু icon এবং style change থাকবে।
- Goal → React এর core concepts ব্যবহার করে একটি ছোট কিন্তু complete site বানানো।

---

2. HW2 System (Learning Framework)
👉 Steps:
1. **How**  
   - প্রথমে শুধু খেয়াল করো → কিভাবে কাজ হচ্ছে।  
   - Step follow করো → কাজ হচ্ছে কিনা দেখো।  
   - এই লেভেলে শুধু HOW এ focus করো।  

2. **What**  
   - Step follow করতে গিয়ে simple mistake হবে।  
   - Mistake খুঁজে বের করো এবং fix করো।  
   - ভুল ঠিক করতে করতে বুঝতে পারবে আসলে WHAT হচ্ছে।  

3. **Why** (Optional)  
   - দ্রুত শিখতে চাইলে চিন্তা করো → কেন এইভাবে করা হচ্ছে।  
   - অন্যভাবে করা যায় কিনা explore করো।  
   - Beginners → এই step skip করতে পারো।  
   - Projects করতে করতে naturally WHY বুঝে যাবে।  

---

3. React Core Concepts Recap
📌 Explanation:
- Component → UI কে ছোট ছোট reusable অংশে ভাগ করা হয়।
- State → Component এর ভিতরে data manage করার জন্য।
- Props → Parent থেকে child এ data পাঠানোর জন্য।
- Event handler → User interaction handle করার জন্য।
- Style change → Dynamic UI তৈরি করার জন্য।

👉 Example:
- Blog component → প্রতিটি blog render করবে।
- Bookmark button → event handler দিয়ে blog কে bookmark list এ যোগ করবে।
- Mark as read → state update করে blog কে read হিসেবে দেখাবে।

---

4. Learning Path
📌 Explanation:
- Module 42 এ আগের সব concepts আবার ব্যবহার করা হবে।
- Component বানানো → আগের মতোই।
- State declare করা → আগের মতোই।
- Props handle করা → আগের মতোই।
- Event handler ব্যবহার করা → আগের মতোই।
- Difference → এবার সব একসাথে combine করে একটি ছোট site বানানো হবে।

---

5. Motivation
📌 Explanation:
- React শেখা → ধাপে ধাপে foundation তৈরি হবে।
- কারো foundation একবারেই clear হবে।
- কারো foundation কয়েকটা layer এ তৈরি হবে।
- নিজের জন্য timeline set করো।
- Extra time দাও → প্রতিদিন ৬-৮ ঘন্টার জায়গায় ১০ ঘন্টা practice করো।
- Future এ এই practice এর value পাবে।

---

6. Output (Example)
👉 Scribbles Café SPA:
- Header → Café title।
- Blog list → প্রতিটি blog এর title, author, reading time।
- Bookmark button → blog কে bookmark list এ যোগ করবে।
- Mark as read button → blog কে read হিসেবে mark করবে।
- Sidebar → Bookmark list এবং read blogs দেখাবে।
- Style → সুন্দর icon এবং CSS দিয়ে সাজানো।

---

📌 Summary
- Module 42 → Simple React SPA বানানো হবে Scribbles Café project দিয়ে।
- HW2 system → How, What, Why framework ব্যবহার করে শেখা।
- React core concepts → component, state, props, event handler আবার ব্যবহার করা হবে।
- Goal → ছোট কিন্তু complete blogging site বানানো।
- Motivation → Practice বেশি করলে foundation strong হবে এবং future এ value পাওয়া যাবে।


===============================
42-1 Scribbles Café Project Overview and Tailwind Setup
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Vite দিয়ে React project setup কিভাবে হয়?
- TailwindCSS এবং DaisyUI setup করার ধাপ কী?
- Navbar component কিভাবে তৈরি করা হয়েছে?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Project Setup
👉 Command:
npm create vite@latest my-blogs -- --template react  

📌 Explanation:
- Vite দিয়ে নতুন React project তৈরি করা হয়েছে।
- Template → react project structure automatically তৈরি হয়।
- Project name → `my-blogs`।

---

2. TailwindCSS Setup
👉 Command:
npm install tailwindcss @tailwindcss/vite  

📌 Explanation:
- TailwindCSS → utility-first CSS framework।
- @tailwindcss/vite → Vite integration plugin।
- vite.config.ts → TailwindCSS plugin যোগ করতে হবে।

👉 vite.config.ts:
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})

📌 Explanation:
- react() → React plugin।
- tailwindcss() → TailwindCSS plugin।
- plugins array → Vite project এ TailwindCSS কাজ করবে।

👉 App.css:
@import "tailwindcss";

📌 Explanation:
- TailwindCSS import করা হয়েছে।
- Tailwind utilities এখন project এ ব্যবহার করা যাবে।

---

3. DaisyUI Setup
👉 Command:
npm i -D daisyui@latest  

👉 App.css:
@plugin "daisyui";

📌 Explanation:
- DaisyUI → TailwindCSS এর উপর ভিত্তি করে তৈরি UI component library।
- @plugin "daisyui" → TailwindCSS এ DaisyUI plugin যোগ করা হয়েছে।
- এখন DaisyUI এর ready-made components ব্যবহার করা যাবে।

---

4. App.jsx
👉 Code:
import './App.css'
import Navbar from './components/Navbar/Navbar'

function App() {
  return (
    <>
      <Navbar></Navbar>
    </>
  )
}
export default App

📌 Explanation (Line by Line):
- import './App.css' → TailwindCSS এবং DaisyUI styles import করা হয়েছে।
- import Navbar → Navbar component import করা হয়েছে।
- <Navbar></Navbar> → Navbar render করা হয়েছে।
- export default App → App component export করা হয়েছে।

---

5. Navbar.jsx
src>components>Navbar>Navbar.jsx
👉 Code:
import React from 'react';

const Navbar = () => {
  return (
    <div>
      <div className="navbar bg-base-100 shadow-sm">
        <div className="flex-1">
          <a className="btn btn-ghost text-xl">daisyUI</a>
        </div>
        <div className="flex-none">
          {/* Cart Dropdown */}
          <div className="dropdown dropdown-end">
            <div tabIndex={0} role="button" className="btn btn-ghost btn-circle">
              <div className="indicator">
                <svg ...> {/* Shopping cart icon */} </svg>
                <span className="badge badge-sm indicator-item">8</span>
              </div>
            </div>
            <div tabIndex={0} className="card card-compact dropdown-content bg-base-100 z-1 mt-3 w-52 shadow">
              <div className="card-body">
                <span className="text-lg font-bold">8 Items</span>
                <span className="text-info">Subtotal: $999</span>
                <div className="card-actions">
                  <button className="btn btn-primary btn-block">View cart</button>
                </div>
              </div>
            </div>
          </div>
          {/* Profile Dropdown */}
          <div className="dropdown dropdown-end">
            <div tabIndex={0} role="button" className="btn btn-ghost btn-circle avatar">
              <div className="w-10 rounded-full">
                <img alt="Tailwind CSS Navbar component"
                  src="https://img.daisyui.com/images/stock/photo-1534528741775-53994a69daeb.webp" />
              </div>
            </div>
            <ul tabIndex="-1" className="menu menu-sm dropdown-content bg-base-100 rounded-box z-1 mt-3 w-52 p-2 shadow">
              <li>
                <a className="justify-between">
                  Profile
                  <span className="badge">New</span>
                </a>
              </li>
              <li><a>Settings</a></li>
              <li><a>Logout</a></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  );
};
export default Navbar;

📌 Explanation (Line by Line):
- <div className="navbar bg-base-100 shadow-sm"> → Navbar container, background এবং shadow।
- <div className="flex-1"> → Left side content (brand name)।
- <a className="btn btn-ghost text-xl">daisyUI</a> → Brand link।
- Cart Dropdown:
  - <svg> → Shopping cart icon।
  - <span className="badge">8</span> → Cart item count।
  - Dropdown content → Items summary এবং "View cart" button।
- Profile Dropdown:
  - <img> → User avatar।
  - <ul> → Dropdown menu with Profile, Settings, Logout।
  - <span className="badge">New</span> → Profile badge।

---

6. Output (Example)
👉 Navbar:
- Left side → Brand name "daisyUI"।
- Right side → Cart icon with indicator (8 items)।
- Dropdown → Cart summary এবং "View cart" button।
- Profile avatar → Dropdown menu with Profile, Settings, Logout।

---

7. Advantages
- TailwindCSS → utility-first CSS, দ্রুত styling।
- DaisyUI → ready-made components, সুন্দর UI।
- Navbar → responsive এবং modern design।
- Dropdowns → cart এবং profile সহজে manage করা যায়।

---

📌 Summary
- Vite দিয়ে React project setup করা হয়েছে।
- TailwindCSS এবং DaisyUI install ও configure করা হয়েছে।
- App.jsx → Navbar component render করছে।
- Navbar.jsx → Cart dropdown এবং Profile dropdown সহ modern navbar তৈরি করা হয়েছে।
- Output → সুন্দর responsive navbar with icons, indicators, এবং dropdown menus।



===============================
42-2 Create Fake Data for Blogs and Load Data
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- blogs.json ফাইল কিভাবে তৈরি করবো?
- JSON data কোথা থেকে পাওয়া যায়?
- React এ useEffect দিয়ে data fetch করার syntax কী?
- useState দিয়ে data কোথায় রাখবো?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Fake Data Creation
👉 Example (public > blogs.json):
[
  {
    "id": 1,
    "name": "jabbar",
    "cover": "bg"
  }
]

📌 Explanation:
- blogs.json → public folder এ রাখা হয়েছে।
- প্রতিটি blog object এ id, name, cover property আছে।
- imgbb → image upload করে direct link JSON data তে ব্যবহার করা যায়।
- JSON data maker → online tool দিয়ে fake data তৈরি করা যায়।

---

2. API Calling Concept
👉 useEffect Syntax:
useEffect(() => {
   // code here
}, [])

📌 Explanation:
- useEffect → React hook, side effect handle করার জন্য।
- [] dependency array → component mount হলে একবার run হবে।
- fetch("blogs.json") → local JSON file থেকে data আনে।

---

3. Blogs.jsx Component
👉 Code:
import React, { useEffect, useState } from 'react';

📌 Explanation:
- React import করা হয়েছে।
- useEffect → data fetch করার জন্য।
- useState → blogs data state এ রাখার জন্য।

---

👉 Code:
const Blogs = () => {
    const [blogs, setBlogs] = useState([]);

📌 Explanation:
- Blogs component → blog data দেখানোর জন্য।
- useState([]) → blogs নামে state তৈরি হয়েছে, শুরুতে empty array।
- setBlogs → blogs state update করার জন্য function।

---

👉 Code (useEffect):
useEffect(() => {
    fetch("blogs.json")
      .then(res => res.json())
      .then(data => setBlogs(data))
}, [])

📌 Explanation (Line by Line):
- useEffect → component mount হলে run হবে।
- fetch("blogs.json") → public folder থেকে blogs.json file fetch করছে।
- .then(res => res.json()) → response কে JSON এ convert করছে।
- .then(data => setBlogs(data)) → blogs state এ data save করছে।
- [] dependency array → শুধু একবার run হবে।

---

👉 Code:
console.log(blogs) // (4) [{…}, {…}, {…}, {…}]

📌 Explanation:
- console.log(blogs) → blogs state এ data আসছে কিনা check করার জন্য।
- Output → array of blog objects।

---

👉 Code:
return (
    <div>
        {/* এখানে blogs data render করা যাবে */}
    </div>
);

📌 Explanation:
- return → UI render করার জায়গা।
- এখনো blogs data দেখানো হয়নি, শুধু empty div আছে।
- Future এ blogs.map() ব্যবহার করে blog list render করা যাবে।

---

👉 Code:
export default Blogs;

📌 Explanation:
- Blogs component export করা হয়েছে।
- অন্য ফাইলে import করে ব্যবহার করা যাবে।

---

4. Output (Example)
👉 Console:
(4) [{id:1, name:"jabbar", cover:"bg"}, {...}, {...}, {...}]  

👉 UI:
এখনো কিছু render হচ্ছে না, শুধু empty div।  
Future এ blog list card আকারে দেখানো যাবে।  

---

5. Advantages
- JSON data → সহজে fake data তৈরি করা যায়।
- useEffect → component mount হলে data fetch করা যায়।
- useState → data state এ রাখা যায় এবং UI তে ব্যবহার করা যায়।
- console.log → data আসছে কিনা verify করা যায়।

---

📌 Summary
- blogs.json → public folder এ fake data রাখা হয়েছে।
- useEffect → component mount হলে blogs.json থেকে data fetch করছে।
- useState → blogs data state এ রাখা হচ্ছে।
- console.log → data আসছে কিনা check করা হচ্ছে।
- Output → blogs data console এ দেখা যাচ্ছে, future এ UI তে render করা যাবে।



===============================
42-3 Set Layout and Make React Responsive
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- useEffect দিয়ে data fetch করার নিয়ম কী?
- useState দিয়ে data কোথায় রাখা হয়?
- Dependency array [] এর কাজ কী?
- React layout responsive কিভাবে করা যায়?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept: useEffect
📌 Explanation:
- useEffect → React hook, side effect handle করার জন্য।
- Component mount হলে useEffect এর ভিতরের code একবার run হয়।
- Dependency array [] → শুধু একবার run হবে।
- যদি dependency array এ কোনো state/variable দেওয়া হয় → সেই value change হলে useEffect আবার run হবে।

👉 Example:
useEffect(() => {
   fetch("blogs.json")
     .then(res => res.json())
     .then(data => setBlogs(data))
}, [])

---

2. Concept: useState
👉 Code:
const [blogs, setBlogs] = useState([]);

📌 Explanation:
- blogs → state variable, যেখানে data রাখা হয়।
- setBlogs → state update করার function।
- শুরুতে blogs = [] (empty array)।
- পরে fetch করা data setBlogs দিয়ে blogs এ রাখা হয়।

---

3. App.jsx Layout
👉 Code:
import './App.css'
import Blogs from './components/Blogs/Blogs'
import Navbar from './components/Navbar/Navbar'

📌 Explanation:
- App.css → global styles import করা হয়েছে।
- Blogs component → blog data দেখানোর জন্য।
- Navbar component → navigation bar দেখানোর জন্য।

---

👉 Code:
function App() {
  return (
    <>
      <Navbar></Navbar>
      <div className="main-container text-center flex">
        <div className="left-container w-[70%]">
          <Blogs></Blogs>
        </div>
        <div className="right-container w-[30%]">
          <h1>Reading time: 0</h1>
          <h1>Bookmarked count: 0</h1>
        </div>
      </div>
    </>
  )
}
export default App

📌 Explanation (Line by Line):
- <Navbar></Navbar> → Navbar render করছে।
- <div className="main-container text-center flex"> → main layout container, flexbox ব্যবহার করা হয়েছে।
- <div className="left-container w-[70%]"> → left side, Blogs component render করছে, width 70%।
- <div className="right-container w-[30%]"> → right side, Reading time এবং Bookmarked count দেখাচ্ছে, width 30%।
- Responsive layout → flexbox এবং Tailwind width classes দিয়ে তৈরি।

---

4. Blogs.jsx Component
👉 Code:
import React, { useEffect, useState } from 'react';

📌 Explanation:
- React import করা হয়েছে।
- useEffect → data fetch করার জন্য।
- useState → blogs data state এ রাখার জন্য।

---

👉 Code:
const Blogs = () => {
    const [blogs, setBlogs] = useState([]);

    useEffect(() => {
        fetch("blogs.json")
          .then(res => res.json())
          .then(data => setBlogs(data))
    }, [])

    console.log(blogs)
    return (
        <div>
            <h1 className='text-3xl'>total: {blogs.length}</h1>
        </div>
    );
};
export default Blogs;

📌 Explanation (Line by Line):
- const [blogs, setBlogs] = useState([]) → blogs state তৈরি হয়েছে।
- useEffect(() => { fetch("blogs.json")... }, []) → blogs.json থেকে data fetch করছে।
- .then(res => res.json()) → response কে JSON এ convert করছে।
- .then(data => setBlogs(data)) → blogs state এ data save করছে।
- console.log(blogs) → blogs data console এ দেখা যাচ্ছে।
- <h1 className='text-3xl'>total: {blogs.length}</h1> → blogs এর সংখ্যা UI তে দেখাচ্ছে।

---

5. Output (Example)
👉 UI:
- Navbar উপরে।
- Left side → Blogs component, total blogs count।
- Right side → Reading time এবং Bookmarked count।
- Responsive layout → flexbox দিয়ে দুই ভাগে ভাগ করা হয়েছে (70% + 30%)।

👉 Console:
- blogs data array দেখা যাবে → [{id:1, name:"jabbar", cover:"bg"}, {...}, {...}]

---

6. Advantages
- useEffect → data fetch করার জন্য perfect।
- useState → data state এ রাখা যায় এবং UI তে ব্যবহার করা যায়।
- Dependency array → control করে কখন useEffect run হবে।
- Flexbox + Tailwind → responsive layout তৈরি করা যায়।
- Clear separation → Navbar, Blogs, এবং Sidebar আলাদা component।

---

📌 Summary
- blogs.json → public folder থেকে data fetch করা হয়েছে।
- useEffect → component mount হলে data fetch করছে।
- useState → blogs data state এ রাখা হচ্ছে।
- App.jsx → responsive layout তৈরি করেছে (Navbar + Blogs + Sidebar)।
- Blogs.jsx → blogs count UI তে দেখাচ্ছে।
- Output → responsive two-column layout, blogs data console এ দেখা যাচ্ছে।



===============================
42-4 Display Blog Data and Hashtags
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- blogs.json থেকে data কিভাবে fetch করবো?
- blogs array কে map করে কিভাবে individual blog দেখাবো?
- Props দিয়ে child component এ data পাঠানোর নিয়ম কী?
- Blog component এ data কিভাবে render হচ্ছে?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept: Array of Objects
📌 Explanation:
- blogs.json → ৪টি object আছে।
- প্রতিটি object → একটি blog এর data (id, title, cover, hashtags ইত্যাদি)।
- blogs state → এই array of objects রাখে।
- blogs.map() → প্রতিটি object কে loop করে individual Blog component এ পাঠায়।

👉 Example:
blogs.map((blog) => <Blog blog={blog}></Blog>)

---

2. Blogs.jsx Component
👉 Code:
import React, { useEffect, useState } from 'react';
import Blog from '../Blog/Blog';

📌 Explanation:
- React import করা হয়েছে।
- useEffect → data fetch করার জন্য।
- useState → blogs data state এ রাখার জন্য।
- Blog component → প্রতিটি blog render করার জন্য।

---

👉 Code:
const Blogs = () => {
    const [blogs, setBlogs] = useState([]);

    useEffect(() => {
        fetch("blogs.json")
          .then(res => res.json())
          .then(data => setBlogs(data))
    }, [])

    console.log(blogs)

📌 Explanation (Line by Line):
- const [blogs, setBlogs] = useState([]) → blogs state তৈরি হয়েছে।
- useEffect(() => {...}, []) → component mount হলে run হবে।
- fetch("blogs.json") → public folder থেকে blogs.json file fetch করছে।
- .then(res => res.json()) → response কে JSON এ convert করছে।
- .then(data => setBlogs(data)) → blogs state এ data save করছে।
- console.log(blogs) → blogs data console এ দেখা যাচ্ছে।

---

👉 Code:
return (
    <div>
        <h1 className='text-3xl'>total: {blogs.length}</h1>
        <div className="all-blogs grid grid-cols-2">
            {
                blogs.map((blog) => <Blog blog={blog}></Blog>)
            }
        </div>
    </div>
);
export default Blogs;

📌 Explanation (Line by Line):
- <h1 className='text-3xl'>total: {blogs.length}</h1> → blogs এর সংখ্যা UI তে দেখাচ্ছে।
- <div className="all-blogs grid grid-cols-2"> → TailwindCSS দিয়ে grid layout (২ column) তৈরি হয়েছে।
- blogs.map((blog) => <Blog blog={blog}></Blog>) → প্রতিটি blog object কে Blog component এ পাঠানো হচ্ছে props আকারে।
- export default Blogs → Blogs component অন্য ফাইলে ব্যবহার করা যাবে।

---

3. Blog.jsx Component
👉 Code:
import React from 'react';

const Blog = ({blog}) => {
    console.log(blog);
    return (
        <div>
            <div className="card bg-base-100 w-96 shadow-sm">
              <figure>
                <img src={blog.cover} alt="Shoes" />
              </figure>
              <div className="card-body">
                <h2 className="card-title">{blog.title}</h2>
                <p>A card component has a figure, a body part, and inside body there are title and actions parts</p>
                <div className="card-actions justify-end">
                  <button className="btn btn-primary">Mark as read</button>
                </div>
              </div>
            </div>
        </div>
    );
};
export default Blog;

📌 Explanation (Line by Line):
- const Blog = ({blog}) → props থেকে blog object পাওয়া যাচ্ছে।
- console.log(blog) → প্রতিটি blog এর data console এ দেখা যাচ্ছে।
- <div className="card bg-base-100 w-96 shadow-sm"> → DaisyUI card component।
- <figure><img src={blog.cover} alt="Shoes" /></figure> → blog এর cover image দেখাচ্ছে।
- <h2 className="card-title">{blog.title}</h2> → blog এর title দেখাচ্ছে।
- <p>...</p> → description placeholder।
- <button className="btn btn-primary">Mark as read</button> → button, future এ event handler যোগ করা যাবে।
- export default Blog → Blog component অন্য ফাইলে ব্যবহার করা যাবে।

---

4. Output (Example)
👉 UI:
- Blogs count → total: 4
- Grid layout → ২ column এ blogs দেখাবে।
- প্রতিটি blog card এ থাকবে:
  - Cover image
  - Title
  - Description
  - "Mark as read" button

👉 Console:
- প্রতিটি blog object data দেখা যাবে।

---

5. Advantages
- blogs.map() → array থেকে প্রতিটি blog আলাদা করে দেখানো যায়।
- Props → parent থেকে child এ data পাঠানো যায়।
- DaisyUI card → সুন্দর UI component।
- Tailwind grid → responsive layout।
- Clear separation → Blogs.jsx (list) এবং Blog.jsx (single item)।

---

📌 Summary
- blogs.json → public folder থেকে data fetch করা হয়েছে।
- useEffect → component mount হলে data fetch করছে।
- useState → blogs data state এ রাখা হচ্ছে।
- Blogs.jsx → blogs.map() দিয়ে Blog component এ data পাঠাচ্ছে।
- Blog.jsx → প্রতিটি blog card আকারে render করছে।
- Output → সুন্দর responsive grid layout এ blogs data UI তে দেখা যাচ্ছে।



===============================
42-5 Add Bookmark Button
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React Icons কিভাবে install এবং ব্যবহার করবো?
- Blog component এ bookmark icon কিভাবে যোগ করবো?
- Props দিয়ে icon এর size কিভাবে নিয়ন্ত্রণ করবো?
- hashtags array কিভাবে render করবো?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. React Icons Setup
👉 Command:
npm install react-icons --save  

👉 Import:
import { FaBookmark } from "react-icons/fa";

📌 Explanation:
- react-icons → জনপ্রিয় icon library।
- FaBookmark → FontAwesome এর bookmark icon।
- <FaBookmark size={25}/> → JSX এ icon render করা যায় এবং props দিয়ে size নিয়ন্ত্রণ করা যায়।

---

2. Blog.jsx Component
👉 Code:
import React from "react";
import { FaBookmark } from "react-icons/fa";

📌 Explanation:
- React import করা হয়েছে।
- FaBookmark → bookmark icon import করা হয়েছে।

---

👉 Code:
const Blog = ({ blog }) => {
  console.log(blog);

📌 Explanation:
- Blog component → props থেকে blog object পাচ্ছে।
- console.log(blog) → প্রতিটি blog এর data console এ দেখা যাচ্ছে।

---

👉 Code:
return (
  <div className="m-2">
    <div className="card bg-base-100 w-96 shadow-sm">
      <figure>
        <img src={blog.cover} alt="Shoes" />
      </figure>

📌 Explanation:
- <div className="m-2"> → margin 2 units।
- <div className="card ..."> → DaisyUI card component।
- <figure><img src={blog.cover} alt="Shoes" /></figure> → blog এর cover image দেখাচ্ছে।

---

👉 Code:
<div className="card-body">
  <div className="author flex justify-around items-center">
    <h3>{blog.author}</h3>
    <img className="w-16" src={blog.author_img} alt="" />
    <FaBookmark size={25}/>
  </div>

📌 Explanation:
- <div className="card-body"> → card এর content অংশ।
- <div className="author flex ..."> → author section, flexbox দিয়ে horizontally সাজানো।
- <h3>{blog.author}</h3> → blog এর author নাম।
- <img className="w-16" src={blog.author_img} /> → author এর image।
- <FaBookmark size={25}/> → bookmark icon, size 25px।

---

👉 Code:
<h2 className="card-title">{blog.title}</h2>
<p>
  A card component has a figure, a body part, and inside body there are title and actions parts
</p>

📌 Explanation:
- <h2 className="card-title">{blog.title}</h2> → blog এর title দেখাচ্ছে।
- <p>...</p> → description placeholder।

---

👉 Code:
<div>
  {
    blog.hashtags.map((has) => <p>{has}</p>)
  }
</div>

📌 Explanation:
- blog.hashtags → array of hashtags।
- .map((has) => <p>{has}</p>) → প্রতিটি hashtag আলাদা <p> tag এ দেখানো হচ্ছে।

---

👉 Code:
<div className="card-actions justify-end">
  <button className="btn btn-primary">Mark as read</button>
</div>

📌 Explanation:
- <div className="card-actions justify-end"> → card এর action section।
- <button className="btn btn-primary">Mark as read</button> → DaisyUI button, future এ event handler যোগ করা যাবে।

---

👉 Code:
export default Blog;

📌 Explanation:
- Blog component export করা হয়েছে।
- অন্য ফাইলে import করে ব্যবহার করা যাবে।

---

3. Output (Example)
👉 UI:
- Blog card → cover image, author name, author image, bookmark icon।
- Title → blog এর শিরোনাম।
- Description → placeholder text।
- Hashtags → array থেকে map করে দেখানো হয়েছে।
- Button → "Mark as read"।

👉 Console:
- প্রতিটি blog object data দেখা যাবে।

---

4. Advantages
- React Icons → সহজে সুন্দর icon যোগ করা যায়।
- Props (size) → icon এর size নিয়ন্ত্রণ করা যায়।
- Hashtags array → map করে dynamicভাবে render করা যায়।
- DaisyUI card → সুন্দর UI structure।
- Clear separation → Blog component reusable।

---

📌 Summary
- React Icons install এবং FaBookmark import করা হয়েছে।
- Blog.jsx → blog data card আকারে render করছে।
- Author section → author name, image, এবং bookmark icon দেখাচ্ছে।
- Hashtags → array থেকে map করে দেখানো হয়েছে।
- Button → "Mark as read" future এ event handler এর জন্য।
- Output → সুন্দর responsive blog card with bookmark icon।



===============================
42-6 Handle Bookmark State and Display Bookmark (Lift Up the State)
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Bookmark click করলে data কোথায় রাখবো?
- State কে central জায়গায় (App.jsx) lift up করার কারণ কী?
- Props দিয়ে function child component এ পাঠানোর নিয়ম কী?
- Bookmark করা data UI তে কিভাবে দেখাবো?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept: Lift Up the State
📌 Explanation:
- Blog component → bookmark icon আছে।
- Blog component → App.jsx এর ভিতরে নেই, বরং Blogs.jsx এর ভিতরে।
- Bookmark click করলে data store করতে হবে।
- State central জায়গায় রাখতে হবে → App.jsx এ।
- কারণ: App.jsx থেকে সব child component এ state share করা যাবে।

---

2. App.jsx Component
👉 Code:
import { useState } from 'react'
import './App.css'
import Blogs from './components/Blogs/Blogs'
import Navbar from './components/Navbar/Navbar'

📌 Explanation:
- useState → bookmark state রাখার জন্য।
- Blogs এবং Navbar component import করা হয়েছে।

---

👉 Code:
function App() {
  const [bookmarked, setBookmarked] = useState([])

  const handleBookMark = (blog) => {
    setBookmarked([...bookmarked, blog])
  }
  console.log(bookmarked)

📌 Explanation (Line by Line):
- const [bookmarked, setBookmarked] = useState([]) → bookmarked state তৈরি হয়েছে, শুরুতে empty array।
- handleBookMark(blog) → bookmark button click করলে blog object state এ যোগ করা হচ্ছে।
- setBookmarked([...bookmarked, blog]) → পুরনো bookmarked array এর সাথে নতুন blog যোগ করা হচ্ছে।
- console.log(bookmarked) → bookmarked state console এ দেখা যাচ্ছে।

---

👉 Code:
return (
  <>
    <Navbar></Navbar>
    <div className="main-container text-center flex">
      <div className="left-container w-[70%]">
        <Blogs handleBookMark={handleBookMark}></Blogs>
      </div>
      <div className="right-container w-[30%]">
        <h1>Reading time: 0</h1>
        <h1>Bookmarked count: 0</h1>
        {
          bookmarked.map((marked) => <p>{marked.title}</p>)
        }
      </div>
    </div>
  </>
)
export default App

📌 Explanation (Line by Line):
- <Navbar></Navbar> → Navbar render করছে।
- <Blogs handleBookMark={handleBookMark}></Blogs> → Blogs component এ handleBookMark function props হিসেবে পাঠানো হয়েছে।
- <div className="right-container"> → sidebar section।
- <h1>Reading time: 0</h1> → placeholder।
- <h1>Bookmarked count: 0</h1> → placeholder।
- bookmarked.map((marked) => <p>{marked.title}</p>) → bookmarked blogs এর title UI তে দেখাচ্ছে।

---

3. Blogs.jsx Component
👉 Code:
import React, { useEffect, useState } from 'react';
import Blog from '../Blog/Blog';

const Blogs = ({handleBookMark}) => {
    const [blogs, setBlogs] = useState([]);

    useEffect(() => {
        fetch("blogs.json").then(res => res.json()).then(data => setBlogs(data))
    }, [])

    return (
        <div>
            <h1 className='text-3xl'>total: {blogs.length}</h1>
            <div className="all-blogs grid grid-cols-2">
                {
                    blogs.map((blog) => <Blog key={blog.id} handleBookMark={handleBookMark} blog={blog}></Blog>)
                }
            </div>
        </div>
    );
};
export default Blogs;

📌 Explanation (Line by Line):
- const [blogs, setBlogs] = useState([]) → blogs state তৈরি হয়েছে।
- useEffect → blogs.json থেকে data fetch করছে।
- blogs.map(...) → প্রতিটি blog কে Blog component এ পাঠাচ্ছে।
- handleBookMark → Blog component এ props হিসেবে পাঠানো হয়েছে।
- key={blog.id} → unique key।

---

4. Blog.jsx Component
👉 Code:
import React from "react";
import { FaBookmark } from "react-icons/fa";

const Blog = ({ blog, handleBookMark }) => {
  return (
    <div className="m-2">
      <div className="card bg-base-100 w-96 shadow-sm">
        <figure>
          <img src={blog.cover} alt="Shoes" />
        </figure>
        <div className="card-body">
          <div className="author flex justify-around items-center">
            <h3>{blog.author}</h3>
            <img className="w-16" src={blog.author_img} alt="" />
            <button onClick={() => handleBookMark(blog)}><FaBookmark size={25}/></button>
          </div>
          <h2 className="card-title">{blog.title}</h2>
          <p>A card component has a figure, a body part, and inside body there are title and actions parts</p>
          <div>
            {blog.hashtags.map((has) => <p>{has}</p>)}
          </div>
          <div className="card-actions justify-end">
            <button className="btn btn-primary">Mark as read</button>
          </div>
        </div>
      </div>
    </div>
  );
};
export default Blog;

📌 Explanation (Line by Line):
- Blog component → props থেকে blog এবং handleBookMark পাচ্ছে।
- <img src={blog.cover}/> → blog এর cover image।
- <h3>{blog.author}</h3> → author name।
- <img src={blog.author_img}/> → author image।
- <button onClick={() => handleBookMark(blog)}><FaBookmark size={25}/></button> → bookmark icon, click করলে blog object App.jsx এ পাঠানো হচ্ছে।
- <h2>{blog.title}</h2> → blog title।
- blog.hashtags.map(...) → hashtags array render করছে।
- <button className="btn btn-primary">Mark as read</button> → read button।

---

5. Output (Example)
👉 UI:
- Left side → Blogs list with bookmark icon।
- Right side → Bookmarked blogs এর title দেখাচ্ছে।
- Bookmark click করলে blog title sidebar এ যোগ হবে।

👉 Console:
- bookmarked array → clicked blog objects দেখা যাবে।

---

6. Advantages
- State lift up → App.jsx এ central state রাখা হয়েছে।
- Props → function child component এ পাঠানো হয়েছে।
- Bookmark click → blog object state এ যোগ হচ্ছে।
- UI → sidebar এ bookmarked blogs দেখানো হচ্ছে।
- Clear separation → App.jsx (state), Blogs.jsx (list), Blog.jsx (single item)।

---

📌 Summary
- Bookmark state central জায়গায় App.jsx এ রাখা হয়েছে।
- handleBookMark function → App.jsx থেকে Blogs এবং Blog এ পাঠানো হয়েছে।
- Bookmark click করলে blog object bookmarked state এ যোগ হচ্ছে।
- Sidebar এ bookmarked blogs এর title দেখানো হচ্ছে।
- Output → responsive layout with bookmark functionality।



===============================
42-7 Add Time When Marked as Read
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- "Mark as read" button এ click করলে কীভাবে reading time যোগ হবে?
- State central জায়গায় (App.jsx) রাখার কারণ কী?
- Props দিয়ে function child component এ পাঠানোর নিয়ম কী?
- Reading time এবং bookmarked count UI তে কিভাবে দেখানো হচ্ছে?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept: Reading Time
📌 Explanation:
- blogs.json → প্রতিটি blog object এ `reading_time` property আছে।
- "Mark as read" button এ click করলে সেই blog এর reading_time central state এ যোগ হবে।
- State central জায়গায় (App.jsx) রাখা হয়েছে যাতে সব child component এ share করা যায়।
- readingCount state → মোট পড়ার সময় track করছে।

---

2. App.jsx Component
👉 Code:
import { useState } from 'react'
import './App.css'
import Blogs from './components/Blogs/Blogs'
import Navbar from './components/Navbar/Navbar'

📌 Explanation:
- useState → state তৈরি করার জন্য।
- Blogs এবং Navbar component import করা হয়েছে।

---

👉 Code:
function App() {
  const [bookmarked, setBookmarked] = useState([])
  const [readingCount, setReadingCount] = useState(0)

📌 Explanation:
- bookmarked → bookmarked blogs এর list।
- readingCount → মোট পড়ার সময় track করছে।
- setBookmarked এবং setReadingCount → state update করার function।

---

👉 Code (handle functions):
const handleBookMark = (blog) => {
  setBookmarked([...bookmarked, blog])
}
console.log(bookmarked)

const handleMarkAsRead = (time) => {
  const newTime = readingCount + time;
  setReadingCount(newTime)
}
console.log(readingCount)

📌 Explanation (Line by Line):
- handleBookMark(blog) → bookmark button click করলে blog object bookmarked state এ যোগ হচ্ছে।
- setBookmarked([...bookmarked, blog]) → পুরনো array এর সাথে নতুন blog যোগ করা হচ্ছে।
- handleMarkAsRead(time) → "Mark as read" button click করলে সেই blog এর reading_time যোগ হচ্ছে।
- const newTime = readingCount + time → পুরনো সময়ের সাথে নতুন সময় যোগ করা হচ্ছে।
- setReadingCount(newTime) → readingCount state update হচ্ছে।
- console.log(bookmarked), console.log(readingCount) → console এ state দেখা যাচ্ছে।

---

👉 Code (return):
return (
  <>
    <Navbar></Navbar>
    <div className="main-container text-center flex">
      <div className="left-container w-[70%]">
        <Blogs handleBookMark={handleBookMark} handleMarkAsRead={handleMarkAsRead}></Blogs>
      </div>
      <div className="right-container w-[30%]">
        <h1>Reading time: {readingCount}</h1>
        <h1>Bookmarked count: {bookmarked.length}</h1>
        {
          bookmarked.map((marked) => <p>{marked.title}</p>)
        }
      </div>
    </div>
  </>
)
export default App

📌 Explanation (Line by Line):
- <Navbar></Navbar> → Navbar render করছে।
- <Blogs handleBookMark={handleBookMark} handleMarkAsRead={handleMarkAsRead}></Blogs> → Blogs component এ functions props হিসেবে পাঠানো হয়েছে।
- <h1>Reading time: {readingCount}</h1> → মোট পড়ার সময় UI তে দেখাচ্ছে।
- <h1>Bookmarked count: {bookmarked.length}</h1> → bookmarked blogs এর সংখ্যা দেখাচ্ছে।
- bookmarked.map(...) → bookmarked blogs এর title UI তে দেখাচ্ছে।

---

3. Blogs.jsx Component
👉 Code:
import React, { useEffect, useState } from 'react';
import Blog from '../Blog/Blog';

const Blogs = ({handleBookMark, handleMarkAsRead}) => {
  const [blogs, setBlogs] = useState([]);

  useEffect(() => {
    fetch("blogs.json").then(res => res.json()).then(data => setBlogs(data))
  }, [])

  return (
    <div>
      <h1 className='text-3xl'>total: {blogs.length}</h1>
      <div className="all-blogs grid grid-cols-2">
        {
          blogs.map((blog) => <Blog key={blog.id} handleBookMark={handleBookMark} handleMarkAsRead={handleMarkAsRead} blog={blog}></Blog>)
        }
      </div>
    </div>
  );
};
export default Blogs;

📌 Explanation (Line by Line):
- blogs state → blogs.json থেকে data fetch করে রাখা হচ্ছে।
- useEffect → component mount হলে data fetch করছে।
- blogs.map(...) → প্রতিটি blog কে Blog component এ পাঠাচ্ছে।
- handleBookMark এবং handleMarkAsRead → Blog component এ props হিসেবে পাঠানো হয়েছে।

---

4. Blog.jsx Component
👉 Code:
import React from "react";
import { FaBookmark } from "react-icons/fa";

const Blog = ({ blog, handleBookMark, handleMarkAsRead }) => {
  return (
    <div className="m-2">
      <div className="card bg-base-100 w-96 shadow-sm">
        <figure>
          <img src={blog.cover} alt="Shoes" />
        </figure>
        <div className="card-body">
          <div className="author flex justify-around items-center">
            <h3>{blog.author}</h3>
            <img className="w-16" src={blog.author_img} alt="" />
            <button onClick={() => handleBookMark(blog)}><FaBookmark size={25}/></button>
          </div>
          <h2 className="card-title">{blog.title}</h2>
          <p>A card component has a figure, a body part, and inside body there are title and actions parts</p>
          <div>
            {blog.hashtags.map((has) => <p>{has}</p>)}
          </div>
          <div className="card-actions justify-end">
            <button onClick={() => handleMarkAsRead(blog.reading_time)} className="btn btn-primary">Mark as read</button>
          </div>
        </div>
      </div>
    </div>
  );
};
export default Blog;

📌 Explanation (Line by Line):
- Blog component → props থেকে blog object এবং functions পাচ্ছে।
- <img src={blog.cover}/> → blog এর cover image।
- <h3>{blog.author}</h3> → author name।
- <img src={blog.author_img}/> → author image।
- <button onClick={() => handleBookMark(blog)}><FaBookmark size={25}/></button> → bookmark icon, click করলে blog object App.jsx এ পাঠানো হচ্ছে।
- <h2>{blog.title}</h2> → blog title।
- blog.hashtags.map(...) → hashtags array render করছে।
- <button onClick={() => handleMarkAsRead(blog.reading_time)} className="btn btn-primary">Mark as read</button> → click করলে blog এর reading_time App.jsx এ পাঠানো হচ্ছে।

---

5. Output (Example)
👉 UI:
- Left side → Blogs list with bookmark and "Mark as read" button।
- Right side → Reading time এবং bookmarked blogs এর title দেখাচ্ছে।
- Bookmark click → blog title sidebar এ যোগ হবে।
- "Mark as read" click → readingCount state এ সময় যোগ হবে।

👉 Console:
- bookmarked array → clicked blog objects দেখা যাবে।
- readingCount → মোট পড়ার সময় দেখা যাবে।

---

6. Advantages
- State lift up → App.jsx এ central state রাখা হয়েছে।
- Props → functions child component এ পাঠানো হয়েছে।
- Bookmark click → blog object bookmarked state এ যোগ হচ্ছে।
- "Mark as read" click → readingCount state এ সময় যোগ হচ্ছে।
- UI → sidebar এ bookmarked blogs এবং মোট পড়ার সময় দেখানো হচ্ছে।

---

📌 Summary
- Bookmark এবং reading time central state এ App.jsx এ রাখা হয়েছে।
- handleBookMark → blog object bookmarked state এ যোগ করছে।
- handleMarkAsRead → blog এর reading_time যোগ করছে।
- Blogs.jsx → Blog component এ functions পাঠাচ্ছে।
- Blog.jsx → bookmark এবং "Mark as read" button দিয়ে event trigger করছে।
- Output → responsive layout with bookmark and reading time functionality।


===============================
42-8 Remove Bookmark After Read
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Bookmark করা blog কে "Mark as read" করলে কিভাবে remove করবো?
- State central জায়গায় (App.jsx) রাখার কারণ কী?
- Props দিয়ে function child component এ পাঠানোর নিয়ম কী?
- Filter ব্যবহার করে bookmarked list থেকে item remove করার নিয়ম কী?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept: Remove Bookmark After Read
📌 Explanation:
- Bookmark করা blog → bookmarked state এ রাখা হয়।
- "Mark as read" button এ click করলে:
  1. সেই blog এর reading_time যোগ হবে।
  2. সেই blog bookmarked list থেকে remove হবে।
- Remove করার জন্য filter ব্যবহার করা হয়েছে → id দিয়ে match করে বাদ দেওয়া হয়েছে।

---

2. App.jsx Component
👉 Code:
import { useState } from 'react'
import './App.css'
import Blogs from './components/Blogs/Blogs'
import Navbar from './components/Navbar/Navbar'

📌 Explanation:
- useState → state তৈরি করার জন্য।
- Blogs এবং Navbar component import করা হয়েছে।

---

👉 Code:
function App() {
  const [bookmarked, setBookmarked] = useState([])
  const [readingCount, setReadingCount] = useState(0)

📌 Explanation:
- bookmarked → bookmarked blogs এর list।
- readingCount → মোট পড়ার সময় track করছে।
- setBookmarked এবং setReadingCount → state update করার function।

---

👉 Code (handle functions):
const handleBookMark = (blog) => {
  setBookmarked([...bookmarked, blog])
}
console.log(bookmarked)

const handleMarkAsRead = (time, id) => {
  const newTime = readingCount + time;
  setReadingCount(newTime)

  console.log(id)
  handleRemoveFromBookmark(id)
}

const handleRemoveFromBookmark = (id) => {
  const remainingBookMark = bookmarked.filter((mark) => mark.id !== id);
  console.log(remainingBookMark)
  setBookmarked(remainingBookMark)
}

📌 Explanation (Line by Line):
- handleBookMark(blog) → bookmark button click করলে blog object bookmarked state এ যোগ হচ্ছে।
- setBookmarked([...bookmarked, blog]) → পুরনো array এর সাথে নতুন blog যোগ করা হচ্ছে।
- handleMarkAsRead(time, id) → "Mark as read" button click করলে:
  - reading_time যোগ হচ্ছে।
  - handleRemoveFromBookmark(id) call হচ্ছে।
- handleRemoveFromBookmark(id) → bookmarked array থেকে সেই id বাদ দিয়ে নতুন array তৈরি করছে।
- setBookmarked(remainingBookMark) → bookmarked state update করছে।

---

👉 Code (return):
return (
  <>
    <Navbar></Navbar>
    <div className="main-container text-center flex">
      <div className="left-container w-[70%]">
        <Blogs handleBookMark={handleBookMark} handleMarkAsRead={handleMarkAsRead}></Blogs>
      </div>
      <div className="right-container w-[30%]">
        <h1>Reading time: {readingCount}</h1>
        <h1>Bookmarked count: {bookmarked.length}</h1>
        {
          bookmarked.map((marked) => <p key={marked.id} className='bg-red-600 p-2 shadow m-2 text-white'>{marked.title}</p>)
        }
      </div>
    </div>
  </>
)
export default App

📌 Explanation (Line by Line):
- <Navbar></Navbar> → Navbar render করছে।
- <Blogs handleBookMark={handleBookMark} handleMarkAsRead={handleMarkAsRead}></Blogs> → Blogs component এ functions props হিসেবে পাঠানো হয়েছে।
- <h1>Reading time: {readingCount}</h1> → মোট পড়ার সময় UI তে দেখাচ্ছে।
- <h1>Bookmarked count: {bookmarked.length}</h1> → bookmarked blogs এর সংখ্যা দেখাচ্ছে।
- bookmarked.map(...) → bookmarked blogs এর title UI তে দেখাচ্ছে, styled with Tailwind classes।

---

3. Blog.jsx Component
👉 Code:
import React from "react";
import { FaBookmark } from "react-icons/fa";

const Blog = ({ blog, handleBookMark, handleMarkAsRead }) => {
  return (
    <div className="m-2">
      <div className="card bg-base-100 w-96 shadow-sm">
        <figure>
          <img src={blog.cover} alt="Shoes" />
        </figure>
        <div className="card-body">
          <div className="author flex justify-around items-center">
            <h3>{blog.author}</h3>
            <img className="w-16" src={blog.author_img} alt="" />
            <button onClick={() => handleBookMark(blog)}><FaBookmark size={25}/></button>
          </div>
          <h2 className="card-title">{blog.title}</h2>
          <p>A card component has a figure, a body part, and inside body there are title and actions parts</p>
          <div>
            {blog.hashtags.map((has) => <p>{has}</p>)}
          </div>
          <div className="card-actions justify-end">
            <button onClick={() => handleMarkAsRead(blog.reading_time, blog.id)} className="btn btn-primary">Mark as read</button>
          </div>
        </div>
      </div>
    </div>
  );
};
export default Blog;

📌 Explanation (Line by Line):
- Blog component → props থেকে blog object এবং functions পাচ্ছে।
- <img src={blog.cover}/> → blog এর cover image।
- <h3>{blog.author}</h3> → author name।
- <img src={blog.author_img}/> → author image।
- <button onClick={() => handleBookMark(blog)}><FaBookmark size={25}/></button> → bookmark icon, click করলে blog object App.jsx এ পাঠানো হচ্ছে।
- <h2>{blog.title}</h2> → blog title।
- blog.hashtags.map(...) → hashtags array render করছে।
- <button onClick={() => handleMarkAsRead(blog.reading_time, blog.id)} className="btn btn-primary">Mark as read</button> → click করলে blog এর reading_time এবং id App.jsx এ পাঠানো হচ্ছে।

---

4. Output (Example)
👉 UI:
- Left side → Blogs list with bookmark and "Mark as read" button।
- Right side → Reading time এবং bookmarked blogs এর title দেখাচ্ছে।
- Bookmark click → blog title sidebar এ যোগ হবে।
- "Mark as read" click → readingCount state এ সময় যোগ হবে এবং সেই blog bookmarked list থেকে remove হবে।

👉 Console:
- bookmarked array → clicked blog objects দেখা যাবে।
- remainingBookMark → filter করার পর নতুন array দেখা যাবে।

---

5. Advantages
- State lift up → App.jsx এ central state রাখা হয়েছে।
- Props → functions child component এ পাঠানো হয়েছে।
- Bookmark click → blog object bookmarked state এ যোগ হচ্ছে।
- "Mark as read" click → readingCount state এ সময় যোগ হচ্ছে এবং bookmarked থেকে remove হচ্ছে।
- Filter → সহজে id দিয়ে item remove করা যায়।
- UI → sidebar এ bookmarked blogs এবং মোট পড়ার সময় দেখানো হচ্ছে।

---

📌 Summary
- Bookmark এবং reading time central state এ App.jsx এ রাখা হয়েছে।
- handleBookMark → blog object bookmarked state এ যোগ করছে।
- handleMarkAsRead → blog এর reading_time যোগ করছে এবং bookmarked থেকে remove করছে।
- handleRemoveFromBookmark → filter দিয়ে bookmarked list থেকে blog remove করছে।
- Blogs.jsx → Blog component এ functions পাঠাচ্ছে।
- Blog.jsx → bookmark এবং "Mark as read" button দিয়ে event trigger করছে।
- Output → responsive layout with bookmark add/remove এবং reading time functionality।


