===============================
39-1 Module Introduction and Vite Project Overview
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Vite project কীভাবে তৈরি হয়?
- React এ component কী?
- JSX কী এবং {} এর মধ্যে কীভাবে JS লেখা যায়?
- Props কীভাবে কাজ করে?
- Conditional Rendering কী এবং কেন দরকার?
- Array loop করে component render করার নিয়ম কী?

📝 Notes (বিস্তারিত তথ্য)

1. Project Vite Setup
- Command → npm create vite@latest react-core-concept-part2 -- --template react
- Vite হলো fast build tool → React project তৈরি করতে ব্যবহার হয়।
- Template হিসেবে React project তৈরি হবে।
- এরপর npm install এবং npm run dev দিয়ে project চালানো যাবে।

2. Component
- Component হলো React এর ছোট unit → শুধু একটি JavaScript function।
- Component reusable → একই UI বারবার ব্যবহার করা যায়।
👉 Example:
function Book(){ return <p>Book</p>; }

3. JSX
- JSX = JavaScript XML → HTML-এর মতো syntax যা JavaScript এর মধ্যে লেখা যায়।
- JSX browser সরাসরি বুঝতে পারে না → Babel compiler এটিকে React.createElement() এ convert করে।
- JSX এ curly braces `{}` ব্যবহার করে JavaScript expression লেখা যায়।
👉 Example:
const name = "Faha";
return <p>Hello, {name}</p>;

4. Props
- Props হলো parent থেকে child component এ data পাঠানোর উপায়।
- Props immutable → child component পরিবর্তন করতে পারে না।
- Destructuring ব্যবহার করে props সহজে পড়া যায়।
👉 Example:
<Book name="Physics" price={250}></Book>
function Book({name, price}){ return <li>{name} - {price}</li>; }

5. Conditional Rendering
- React এ শর্ত অনুযায়ী UI পরিবর্তন করা যায়।
- ৬টি পদ্ধতি → if statement, return null, ternary operator (? :), logical AND (&&), logical OR (||), variable assignment।
👉 Example:
isDone ? <p>Done</p> : <p>Pending</p>;

6. Array Loop with map()
- Array এর উপর map() ব্যবহার করে প্রতিটি element কে component এ পাঠানো যায়।
👉 Example:
books.map(book => <Book key={book.id} book={book}></Book>)

📌 Explanation:
- books array এর প্রতিটি object Book component এ পাঠানো হচ্ছে।
- key prop → unique identity দেয়, performance বাড়ায়।

📌 Summary
- Vite project setup → দ্রুত React project তৈরি করার জন্য।
- Component → ছোট unit, reusable।
- JSX → HTML-এর মতো syntax, curly braces দিয়ে JS expression।
- Props → parent থেকে child এ data পাঠানোর উপায়।
- Conditional Rendering → ৬টি পদ্ধতিতে করা যায়।
- Array loop → map() ব্যবহার করে component render করা যায়।


===============================
39-2 Different Ways to Handle Events in React
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ event handling কীভাবে কাজ করে?
- HTML event handler vs React event handler এর পার্থক্য কী?
- Event handler যোগ করার বিভিন্ন উপায় কী কী?
- Event handler এর নিয়ম এবং pitfalls কী?
- কিভাবে parameter pass করা যায়?
- Props event handler এ কিভাবে পড়া যায়?
- Event propagation এবং preventDefault কীভাবে কাজ করে?

📝 Notes (বিস্তারিত তথ্য)

1. Responding to Events
- React এ event handling হলো user interaction (click, input, submit ইত্যাদি) এর response।
- Event handler হলো function যা নির্দিষ্ট event ঘটলে execute হয়।

2. HTML Event Handler vs React Event Handler
- HTML → inline attribute ব্যবহার করে event handle করা হয়।
👉 Example (HTML):
<button onclick="handleClick()">Click</button>

- React → camelCase attribute ব্যবহার করে function pass করা হয়।
👉 Example (React):
<button onClick={handleClick}>Click</button>

📌 Difference:
- HTML এ string হিসেবে function call করা হয়।
- React এ function reference pass করা হয়।

3. Adding Event Handler (Different Ways)
👉 Way 1: Normal function
function handleClick(){ alert('I am clicked'); }
<button onClick={handleClick}>Click Me</button>

👉 Way 2: Inline function
<button onClick={function handleClick2(){ alert('clicked 2'); }}>Click Me2</button>

👉 Way 3: Arrow function
const handleClick3 = () => { alert('clicked 3'); }
<button onClick={handleClick3}>Click Me3</button>

👉 Way 4: Inline arrow function (anonymous)
<button onClick={() => alert('click4')}>Click Me4</button>

👉 Way 5: Passing parameters
const handleAdd5 = (num) => { alert(num + 5); }
<button onClick={() => handleAdd5(7)}>Click Add 5</button>

4. Rules of Adding Event Handler
- Function reference pass করতে হবে, call নয়।
👉 Wrong: onClick={handleClick()}  
👉 Correct: onClick={handleClick}

- Attribute নাম camelCase হতে হবে (onClick, onChange, onSubmit)।

5. Pitfalls
- Function call করে দিলে event handler সাথে সাথে execute হয়ে যায়।
- Inline function বেশি ব্যবহার করলে performance কমে যেতে পারে।
- Nested event propagation control না করলে unexpected behavior হতে পারে।

6. Passing Parameters
- Arrow function ব্যবহার করে parameter pass করা যায়।
👉 Example:
<button onClick={() => handleAdd5(7)}>Click Add 5</button>

7. Reading Props in Event Handlers
👉 Example:
function Button({label}){
  const handleClick = () => alert(label);
  return <button onClick={handleClick}>{label}</button>;
}

📌 Explanation:
- Props event handler এর মধ্যে ব্যবহার করা যায়।

8. Event Propagation
- Event bubbling → child থেকে parent পর্যন্ত event propagate হয়।
- stopPropagation() → propagation বন্ধ করে।
👉 Example:
event.stopPropagation();

9. Preventing Default Behaviour
- preventDefault() → default browser action বন্ধ করে।
👉 Example:
function handleSubmit(event){
  event.preventDefault();
  alert('Form submitted without reload');
}

📌 Explanation:
- Form submit করলে browser reload হয় → preventDefault() দিয়ে তা বন্ধ করা যায়।

📌 Summary
- React এ event handler camelCase attribute দিয়ে function reference হিসেবে pass করা হয়।
- Event handler যোগ করার উপায় → normal function, inline function, arrow function, inline arrow, parameter passing।
- Rules → function reference দিতে হবে, camelCase ব্যবহার করতে হবে।
- Pitfalls → function call করলে সাথে সাথে execute হয়, inline function বেশি ব্যবহার করলে performance কমে।
- Parameter pass → arrow function দিয়ে।
- Props event handler এ পড়া যায়।
- Event propagation → stopPropagation() দিয়ে control করা যায়।
- preventDefault() → default browser action বন্ধ করা যায়।


===============================
39-3 A Simple Introduction to State Change in React
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ state কী?
- State change কিভাবে কাজ করে?
- useState hook কীভাবে কাজ করে?
- Destructuring এর মাধ্যমে state এবং setter function কিভাবে পাওয়া যায়?
- Counter component এ state change কিভাবে implement করা হয়েছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept of State
- State হলো React component এর ভিতরের data যা সময়ের সাথে পরিবর্তন হতে পারে।
- Props → বাইরে থেকে আসে (read-only), State → ভিতরে থাকে (mutable via setState)।
- State change হলে React component পুনরায় render হয়।

2. Destructuring Example
👉 Example:
const [first, second] = [2, 5];
console.log(first); // 2
console.log(second); // 5

📌 Explanation:
- Array destructuring করে একাধিক মান আলাদা variable এ রাখা যায়।
- React এর useState hook একইভাবে কাজ করে → [state, setState]।

3. Custom useState Simulation
👉 Example:
function useState(initialValue) {
    let state = initialValue; 
    function setState(newValue) {
        state = newValue;
    }
    return [state, setState];
}

📌 Explanation:
- useState আসলে একটি function যা state এবং setState function return করে।
- React এর আসল useState hook component কে পুনরায় render করে, কিন্তু custom function শুধু value পরিবর্তন করে।

4. React useState Hook
👉 Syntax:
const [state, setState] = useState(initialState);

📌 Explanation:
- state → বর্তমান মান।
- setState → state update করার function।
- initialState → state এর প্রাথমিক মান।

5. Example Project Code

👉 App.jsx
import './App.css'
import Counter from './Counter';

function App() {
  return (
    <>
      <h1>Vite + React</h1>
      <Counter></Counter>
    </>
  )
}
export default App;

👉 Counter.jsx
import { useState } from "react"

export default function Counter(){
    const [count, setCount] = useState(0);

    const handleAdd = () => {
        const newCount = count + 1;
        setCount(newCount);
    }

    const counterStyle = {
        border: '2px solid yellow'
    }

    return (
        <div style={counterStyle}>
            <h3>Count: {count}</h3>
            <button onClick={handleAdd}>Add</button>
        </div>
    )
}

📌 Explanation:
- Counter component এ count নামক state আছে।
- Initial value → 0।
- handleAdd function → count + 1 করে নতুন মান set করে।
- setCount(newCount) → state update করে এবং UI পুনরায় render হয়।

6. Output
React Core Concepts  
Count: 0  
[Add button] → প্রতিবার click করলে count 1 করে বাড়বে।

7. Important Notes
- State change করলে component পুনরায় render হয়।
- setState asynchronous হতে পারে → তাই নতুন মান calculate করার সময় সতর্ক থাকতে হবে।
- Docs পড়ার অভ্যাস ভালো → React official docs এ useState বিস্তারিতভাবে দেওয়া আছে।

📌 Summary
- State হলো React component এর ভিতরের পরিবর্তনশীল data।
- useState hook → [state, setState] return করে।
- Destructuring → state এবং setter function আলাদা করে পাওয়া যায়।
- Counter component এ state change → button click করলে count বাড়ে।
- State change UI কে dynamic করে তোলে।


===============================
39-4 Understand the Concept of useState and React Hooks
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- useState() কী এবং কেন দরকার?
- State আসলে কী?
- useState hook কিভাবে লেখা হয়?
- State ছাড়া value update করলে কী হয়?
- State snapshot কীভাবে কাজ করে?
- Multiple state variables কিভাবে দেওয়া যায়?
- useState এর initialization এর ধরন কী কী?
- Updater function কিভাবে কাজ করে?
- useState এর ৫টি সাধারণ ব্যবহার কী কী?

📝 Notes (বিস্তারিত তথ্য)

1. What Exactly is State?
- State হলো React component এর ভিতরের data যা সময়ের সাথে পরিবর্তন হতে পারে।
- Props → বাইরে থেকে আসে (read-only), State → ভিতরে থাকে (mutable via setState)।
- State change হলে React component পুনরায় render হয়।

2. How to Write a useState Hook?
👉 Syntax:
const [state, setState] = useState(initialState);

📌 Explanation:
- state → বর্তমান মান।
- setState → state update করার function।
- initialState → state এর প্রাথমিক মান।

3. Updating Value Without State
- শুধু variable update করলে React UI পরিবর্তন করে না।
👉 Example:
let count = 0; count++; // UI তে পরিবর্তন হবে না
- setState ব্যবহার করলে UI re-render হয়।

4. Why useState() is Needed?
- UI কে dynamic করার জন্য।
- State change হলে React UI পুনরায় render করে।
- User interaction অনুযায়ী data পরিবর্তন হয়।

5. State as a Snapshot
- State হলো একটি snapshot → প্রতিবার render এর সময় state এর মান আলাদা থাকে।
- setState asynchronous → তাই নতুন মান calculate করার সময় সতর্ক থাকতে হয়।

6. Multiple State Variables
👉 Example:
const [runs, setRuns] = useState(0);
const [sixes, setSixes] = useState(0);

📌 Explanation:
- একাধিক useState ব্যবহার করে component এ একাধিক state রাখা যায়।

7. Initialization Types in useState()
- Primitive values → number, string, boolean
- Object → useState({name: "Rahim"})
- Array → useState([1,2,3])
- Function initialization → useState(() => expensiveCalculation())

8. Updater Function (Under the Hood)
👉 Example:
setState(prev => prev + 1);

📌 Explanation:
- setState function asynchronous → previous state ব্যবহার করে নতুন মান calculate করা যায়।

9. Example Project Code

👉 App.jsx
import './App.css'
import Batsman from './Batsman';

function App() {
  return (
    <>
      <h1>Vite + React</h1>
      <Batsman></Batsman>
    </>
  )
}
export default App;

👉 Batsman.jsx
import { useState } from "react";

export default function Batsman() {
    const [runs, setRuns] = useState(0);
    const [sixes, setSixes] = useState(0);

    const handleSingle = () => {
        setRuns(runs + 1);
    }

    const handleSix = () => {
        setRuns(runs + 6);
        setSixes(sixes + 1);
    }

    const handleFour = () => {
        setRuns(runs + 4);
    }

    return (
        <div>
            <h3>Player: Bangla</h3>
            <p><small>Six: {sixes}</small></p>
            <h1>Score: {runs}</h1>
            {runs > 50 && <p>You score: 50</p>}
            <button onClick={handleSingle}>singles</button>
            <button onClick={handleSix}>Six</button>
            <button onClick={handleFour}>Four</button>
        </div>
    )
}

📌 Output:
Player: Bangla  
Six: 21  
Score: 146  
You score: 50  

Buttons → singles | Six | Four

10. 5 Use Cases of useState()
1. State management → dynamic data handle করা।
2. Conditional rendering → শর্ত অনুযায়ী UI পরিবর্তন।
3. Toggle flags (true/false) → যেমন modal show/hide।
4. Counter → সংখ্যা বাড়ানো/কমানো।
5. Store API data in state → API থেকে data এনে state এ রাখা।

📌 Summary
- State হলো React component এর ভিতরের পরিবর্তনশীল data।
- useState hook → [state, setState] return করে।
- State ছাড়া value update করলে UI পরিবর্তন হয় না।
- State snapshot → প্রতিবার render এ নতুন মান।
- Multiple state variables → একাধিক useState ব্যবহার করা যায়।
- Initialization → primitive, object, array, function।
- Updater function → previous state ব্যবহার করে নতুন মান calculate করা যায়।
- useState এর ৫টি ব্যবহার → state management, conditional rendering, toggle, counter, API data।



===============================
39-6 Load Dynamic Data, API Call Using use
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ dynamic data কিভাবে load করা যায়?
- API call করার নিয়ম কী?
- Suspense এবং fallback কীভাবে কাজ করে?
- use() hook এর ব্যবহার কী?
- Users এবং Friends component এ data কিভাবে render হচ্ছে?

📝 Notes (বিস্তারিত তথ্য)

1. Dynamic Data Loading
- React এ dynamic data load করার জন্য API call ব্যবহার করা হয়।
- fetch() → API থেকে data আনার জন্য।
- Promise resolve হলে data পাওয়া যায়।
👉 Example:
fetch("https://jsonplaceholder.typicode.com/users")
  .then(res => res.json())
  .then(data => console.log(data));

2. Async/Await for API
👉 Example:
const fetchFriends = async () => {
  const res = await fetch('https://jsonplaceholder.typicode.com/users');
  return res.json();
}

📌 Explanation:
- async/await → asynchronous code সহজে পড়ার মতো করে লেখা যায়।
- fetchFriends() একটি promise return করে।

3. Suspense and Fallback
- Suspense → asynchronous component load করার সময় fallback UI দেখায়।
- fallback → loading message বা অন্য UI।
👉 Example:
<Suspense fallback={<h3>Loading.....</h3>}>
   <Users fetchUsers={fetchUsers}></Users>
</Suspense>

📌 Explanation:
- Users component load হওয়ার আগে fallback message দেখাবে।

4. use() Hook
- use() হলো React এর নতুন hook যা promise resolve করে data return করে।
- Suspense এর সাথে কাজ করে → promise resolve না হওয়া পর্যন্ত fallback দেখায়।
👉 Example:
const users = use(fetchUsers);

📌 Explanation:
- fetchUsers promise resolve হলে users data পাওয়া যায়।

5. Example Project Code

👉 App.jsx
import { Suspense } from 'react'
import './App.css'
import Users from './Users'
import Friends from './Friends'

const fetchUsers = fetch('https://jsonplaceholder.typicode.com/users')
  .then(res => res.json());

const fetchFriends = async () => {
  const res = await fetch('https://jsonplaceholder.typicode.com/users');
  return res.json();
}

function App() {
  const friendsPromise = fetchFriends();
  return (
    <>
      <h1>Vite + React</h1>
      <Suspense fallback={<h3>Loading.....</h3>}>
        <Users fetchUsers={fetchUsers}></Users>
      </Suspense>
      <Suspense fallback={<h3>Friends are coming for treat</h3>}>
        <Friends friendsPromise={friendsPromise}></Friends>
      </Suspense>
    </>
  )
}
export default App;

👉 Users.jsx
import { use } from "react"

export default function Users({fetchUsers}){
  const users = use(fetchUsers);
  console.log(users);
  return (
    <div className="card">
      <h3>User: {users.length}</h3>
    </div>
  )
}

👉 Friends.jsx
import { use } from "react"

export default function Friends({friendsPromise}){
  const friends = use(friendsPromise);
  console.log(friends);
  return (
    <div className="card">
      <h3>Friends: {friends.length}</h3>
    </div>
  )
}

6. Output
User: 10  
Friends: 10  

📌 Explanation:
- API থেকে 10 জন user data এসেছে।
- Users এবং Friends component এ length দেখানো হয়েছে।

7. Advantages
- Suspense → asynchronous data load করার সময় fallback UI দেয়।
- use() → promise resolve করে সহজে data ব্যবহার করা যায়।
- কোড clean এবং readable হয়।
- API data সহজে UI তে দেখানো যায়।

📌 Summary
- Dynamic data load → fetch() বা async/await দিয়ে API call।
- Suspense → fallback UI দেখায় যতক্ষণ না data আসে।
- use() hook → promise resolve করে data return করে।
- Users এবং Friends component API data render করছে।
- Output → User: 10, Friends: 10।


===============================
39-7 (Recap) Load Dynamic Data Using async/await
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ dynamic data কিভাবে load করা যায়?
- async/await দিয়ে API call করার নিয়ম কী?
- Suspense এবং fallback কীভাবে কাজ করে?
- use() hook এর ব্যবহার কী?
- Users, Friends, এবং Posts component এ data কিভাবে render হচ্ছে?

📝 Notes (বিস্তারিত তথ্য)

1. Dynamic Data Loading
- React এ dynamic data load করার জন্য API call ব্যবহার করা হয়।
- fetch() → API থেকে data আনার জন্য।
- async/await → asynchronous code সহজে পড়ার মতো করে লেখা যায়।
👉 Example:
const fetchPosts = async () => {
  const res = await fetch("https://jsonplaceholder.typicode.com/posts");
  return res.json();
}

2. Suspense and Fallback
- Suspense → asynchronous component load করার সময় fallback UI দেখায়।
- fallback → loading message বা অন্য UI।
👉 Example:
<Suspense fallback={<h3>Loading.....</h3>}>
   <Users fetchUsers={fetchUsers}></Users>
</Suspense>

📌 Explanation:
- Users component load হওয়ার আগে fallback message দেখাবে।

3. use() Hook
- use() হলো React এর নতুন hook যা promise resolve করে data return করে।
- Suspense এর সাথে কাজ করে → promise resolve না হওয়া পর্যন্ত fallback দেখায়।
👉 Example:
const users = use(fetchUsers);

📌 Explanation:
- fetchUsers promise resolve হলে users data পাওয়া যায়।

4. Example Project Code

👉 App.jsx
import { Suspense } from 'react'
import './App.css'
import Users from './Users'
import Friends from './Friends'
import Posts from './Posts'

const fetchUsers = fetch('https://jsonplaceholder.typicode.com/users')
  .then(res => res.json());

const fetchFriends = async () => {
  const res = await fetch('https://jsonplaceholder.typicode.com/users');
  return res.json();
}

const fetchPosts = async () => {
  const res = await fetch("https://jsonplaceholder.typicode.com/posts");
  return res.json();
}

function App() {
  const friendsPromise = fetchFriends();
  const postsPromise = fetchPosts();

  return (
    <>
      <h1>Vite + React</h1>
      <Suspense fallback={<h3>Loading.....</h3>}>
        <Users fetchUsers={fetchUsers}></Users>
      </Suspense>
      <Suspense fallback={<h3>Friends are coming for treat</h3>}>
        <Friends friendsPromise={friendsPromise}></Friends>
      </Suspense>
      <Suspense fallback={<h4>Posts are coming.......</h4>}>
        <Posts postsPromise={postsPromise}></Posts>
      </Suspense>
    </>
  )
}
export default App;

👉 Users.jsx
import { use } from "react"
export default function Users({fetchUsers}){
  const users = use(fetchUsers);
  return (
    <div className="card">
      <h3>User: {users.length}</h3>
    </div>
  )
}

👉 Friends.jsx
import { use } from "react"
import Friend from "./Friend";
export default function Friends({friendsPromise}){
  const friends = use(friendsPromise);
  return (
    <div className="card">
      <h3>Friends: {friends.length}</h3>
      {friends.map(friend => <Friend key={friend.id} friend={friend}></Friend>)}
    </div>
  )
}

👉 Friend.jsx
export default function Friend({friend}){
  const {name, email, phone} = friend;
  return (
    <div className="card">
      <h4>Name: {name}</h4>
      <p>Email: {email}</p>
      <p>Phone: {phone}</p>
    </div>
  )
}

👉 Posts.jsx
import { use } from "react"
import Post from "./Post"
export default function Posts({postsPromise}){
  const posts = use(postsPromise);
  return (
    <div className="card">
      <h2>All posts are here {posts.length}</h2>
      {posts.map(post => <Post key={post.id} post={post}></Post>)}
    </div>
  )
}

👉 Post.jsx
export default function Post({post}){
  return (
    <div className="card">
      <h5>{post.title}</h5>
      <p>{post.body}</p>
    </div>
  )
}

5. Output
User: 10  
Friends: 10  
Name: Leanne Graham  
Email: Sincere@april.biz  
Phone: 1-770-736-8031 x56442  

All posts are here 100  
sunt aut facere repellat provident occaecati excepturi optio reprehenderit  
quia et suscipit suscipit recusandae consequuntur expedita et cum reprehenderit molestiae ut ut quas totam nostrum rerum est autem sunt rem eveniet architecto  

6. Advantages
- async/await → asynchronous code সহজে পড়া যায়।
- Suspense → fallback UI দেখায় যতক্ষণ না data আসে।
- use() → promise resolve করে সহজে data ব্যবহার করা যায়।
- Component-based structure → Users, Friends, Posts আলাদা আলাদা data handle করছে।
- কোড clean, modular এবং readable হয়।

📌 Summary
- Dynamic data load → async/await দিয়ে API call।
- Suspense → fallback UI দেখায় যতক্ষণ না data আসে।
- use() hook → promise resolve করে data return করে।
- Users, Friends, এবং Posts component API data render করছে।
- Output → User: 10, Friends: 10, Posts: 100।


===============================
39-8 (Optional) Load Data Using useEffect Hook
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- useEffect hook কী?
- কেন useEffect দরকার?
- API call কিভাবে useEffect দিয়ে করা যায়?
- useState এবং useEffect একসাথে কিভাবে কাজ করে?
- Players component এ data কিভাবে render হচ্ছে?

📝 Notes (বিস্তারিত তথ্য)

1. What is useEffect?
- useEffect হলো React এর একটি hook যা side-effect handle করতে ব্যবহৃত হয়।
- Side-effect → API call, event listener, DOM manipulation ইত্যাদি।
- useEffect component render হওয়ার পর execute হয়।

👉 Syntax:
useEffect(() => {
   // side-effect code
}, [dependencies]);

📌 Explanation:
- প্রথম parameter → callback function (side-effect code)।
- দ্বিতীয় parameter → dependency array (কোন state পরিবর্তন হলে effect পুনরায় চালু হবে)।

2. Why useEffect is Needed?
- Component render হওয়ার পর API call করার জন্য।
- State পরিবর্তন হলে নির্দিষ্ট কাজ পুনরায় চালানোর জন্য।
- Lifecycle এর মতো কাজ করে → mount, update, unmount।

3. API Call with useEffect
👉 Example:
useEffect(() => {
   fetch('https://jsonplaceholder.typicode.com/users')
     .then(res => res.json())
     .then(data => setPlayers(data));
}, []);

📌 Explanation:
- fetch() → API থেকে data আনে।
- .then() → promise resolve হলে data পাওয়া যায়।
- setPlayers(data) → state update করে।
- Dependency array [] → শুধু প্রথমবার render হলে effect চালু হবে।

4. Example Project Code

👉 App.jsx
import './App.css'
import Players from './Players'

function App() {
  return (
    <>
      <h1>Vite + React</h1>
      <Players></Players>
    </>
  )
}
export default App;

👉 Players.jsx
import { useEffect, useState } from "react"

export default function Players(){
  const [players, setPlayers] = useState([]);

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/users')
      .then(res => res.json())
      .then(data => setPlayers(data));
  }, []);

  return (
    <div className="card">
      <h4>Players {players.length}</h4>
      <ol>
        {players.map(player => <li key={player.id}>{player.name}</li>)}
      </ol>
    </div>
  )
}

📌 Correction:
- useEffect এ dependency array [] দেওয়া উচিত, না হলে প্রতিবার render এ API call হবে।
- players.map এ key={player.id} দেওয়া উচিত unique key এর জন্য।

5. Output
Players 10  
Leanne Graham  
Ervin Howell  
Clementine Bauch  
Patricia Lebsack  
Chelsey Dietrich  
Mrs. Dennis Schulist  
Kurtis Weissnat  
Nicholas Runolfsdottir V  
Glenna Reichert  
Clementina Dubuque  

6. Advantages
- useEffect দিয়ে API call সহজে করা যায়।
- State update হলে UI dynamic হয়।
- Dependency array দিয়ে control করা যায় কখন effect চালু হবে।
- Component clean এবং predictable হয়।

📌 Summary
- useEffect → side-effect handle করার জন্য React hook।
- API call → useEffect এর মধ্যে fetch() ব্যবহার করা হয়।
- useState → API data state এ রাখা হয়।
- Players component → API থেকে data এনে list আকারে দেখাচ্ছে।
- Dependency array [] → শুধু প্রথমবার render হলে API call হবে।


