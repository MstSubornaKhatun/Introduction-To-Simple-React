===============================
Module 40: Simple React Rest Countries
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এর মূল ছয়টি জিনিস কী?
- JSX কীভাবে কাজ করে?
- Component কী এবং কেন দরকার?
- Props কীভাবে data pass করে?
- Events কীভাবে handle করা হয়?
- State এবং Effect এর ভূমিকা কী?

📝 Notes (বিস্তারিত তথ্য)

1. JSX
- JSX = JavaScript XML → HTML-এর মতো syntax যা JavaScript এর মধ্যে লেখা যায়।
- JSX browser সরাসরি বুঝতে পারে না → Babel এটিকে React.createElement() এ convert করে।
👉 Example:
return <h1>Hello React</h1>;

2. Component
- Component হলো React এর ছোট unit → শুধু একটি JavaScript function।
- Component reusable → একই UI বারবার ব্যবহার করা যায়।
👉 Example:
function Country(){ return <p>Bangladesh</p>; }

3. Props
- Props হলো parent থেকে child component এ data পাঠানোর উপায়।
- Props immutable → child component পরিবর্তন করতে পারে না।
👉 Example:
<Country name="Bangladesh"></Country>
function Country({name}){ return <p>{name}</p>; }

4. Events
- Events হলো user interaction (click, input, submit ইত্যাদি)।
- React এ event handler camelCase attribute দিয়ে function reference হিসেবে pass করা হয়।
👉 Example:
<button onClick={handleClick}>Click Me</button>

5. State
- State হলো React component এর ভিতরের data যা সময়ের সাথে পরিবর্তন হতে পারে।
- State change হলে React component পুনরায় render হয়।
👉 Example:
const [count, setCount] = useState(0);

6. Effect
- useEffect হলো React এর hook যা side-effect handle করতে ব্যবহৃত হয়।
- Side-effect → API call, event listener, DOM manipulation।
👉 Example:
useEffect(() => {
   fetch('https://restcountries.com/v3.1/all')
     .then(res => res.json())
     .then(data => setCountries(data));
}, []);

7. Side Things (Extra)
- Key props → list rendering এ unique identity দেয়।
- Conditional rendering → শর্ত অনুযায়ী UI পরিবর্তন করা যায়।
- Map() → array loop করে component render করা যায়।

📌 Summary
- React এর মূল ছয়টি জিনিস → JSX, Component, Props, Events, State, Effect।
- এগুলো দিয়েই পুরো সাইট তৈরি হয়।
- JSX → HTML-এর মতো syntax।
- Component → ছোট unit, reusable।
- Props → parent থেকে child এ data পাঠানো।
- Events → user interaction handle করা।
- State → পরিবর্তনশীল data।
- Effect → side-effect যেমন API call।
- Extra → key props, conditional rendering, map()।


===============================
40-1 Module Introduction: Load Countries Using Promise and API
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React project কিভাবে তৈরি করতে হয়?
- Rest Countries API কীভাবে ব্যবহার করা যায়?
- React component declare করার দুইটি উপায় কী?
- Arrow function কেন best practice?
- Promise দিয়ে API data কিভাবে load করা যায়?

📝 Notes (বিস্তারিত তথ্য)

1. Create React Project
👉 Command:
npm create vite@latest react-world-on-the-go -- --template react

📌 Explanation:
- Vite → fast build tool, React project তৈরি করতে ব্যবহার হয়।
- Project name → react-world-on-the-go।
- Template → React project structure তৈরি হবে।

2. Rest Countries API
👉 API URL:
https://restcountries.com/v3.1/all

📌 Explanation:
- এই API থেকে সব দেশের data পাওয়া যায়।
- Data → name, capital, population, flags ইত্যাদি।

3. Component Declaration
👉 Normal Function:
function Countries() {
  return (
    <div>
    </div>
  )
}

👉 Arrow Function:
const Countries = () => {
  return (
    <div>
    </div>
  )
}

📌 Explanation:
- দুইভাবে component declare করা যায়।
- Arrow function → concise, modern JavaScript style।
- Best practice → arrow function ব্যবহার করা।

4. Promise দিয়ে API Call
👉 Example:
fetch("https://restcountries.com/v3.1/all")
  .then(res => res.json())
  .then(data => console.log(data));

📌 Explanation:
- fetch() → API থেকে data আনে।
- .then() → promise resolve হলে data পাওয়া যায়।
- console.log(data) → data দেখতে পাওয়া যায়।

5. React Extension Pack
- VS Code এ React Extension Pack install করলে development সহজ হয়।
- Features → syntax highlighting, snippets, linting, auto-completion।

6. Advantages
- Vite দিয়ে project setup দ্রুত হয়।
- Rest Countries API → free এবং সহজে ব্যবহারযোগ্য।
- Arrow function → clean এবং readable code।
- Promise → asynchronous data handle করা যায়।

📌 Summary
- React project setup → vite দিয়ে।
- API → Rest Countries থেকে data আনা যায়।
- Component declare → normal function বা arrow function।
- Arrow function → best practice।
- Promise → asynchronous API data load করার জন্য।
- React Extension Pack → development সহজ করে।


===============================
40-2 Display Countries in a Simple Way
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ কিভাবে countries data display করা যায়?
- Suspense এবং fallback কীভাবে কাজ করে?
- use() hook এর ভূমিকা কী?
- Component hierarchy কেমন হবে?
- Code এর প্রতিটি অংশ কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. App.jsx
👉 Code:
import { Suspense } from "react";
import "./App.css";
import Countries from "./components/Countries/Countries";

const countriesPromise = fetch("https://restcountries.com/v3.1/all").then(res => res.json());

function App() {
  return (
    <>
      <Suspense fallback={<h1>Loading..........</h1>}>
        <Countries countriesPromise={countriesPromise}></Countries>
      </Suspense>
    </>
  );
}
export default App;

📌 Explanation:
- countriesPromise → fetch() দিয়ে Rest Countries API থেকে সব দেশের data আনা হচ্ছে।
- Suspense → asynchronous data load হওয়ার সময় fallback UI দেখায়।
- Countries component → countriesPromise props হিসেবে পাচ্ছে।

2. Countries.jsx
👉 Code:
import React, { use } from 'react';
import Country from '../Country/Country';

const Countries = ({countriesPromise}) => {
  const countries = use(countriesPromise);
  console.log(countries);
  return (
    <div>
      <h1>Traveling Countries: {countries.length}</h1>
      {countries.map(country => <Country key={country.cca3} country={country}></Country>)}
    </div>
  );
};
export default Countries;

📌 Explanation:
- use(countriesPromise) → promise resolve করে countries data return করছে।
- countries.length → মোট কতগুলো country আছে তা দেখাচ্ছে।
- countries.map() → প্রতিটি country কে Country component এ পাঠাচ্ছে।
- key={country.cca3} → প্রতিটি list item এর unique identity (best practice)।

3. Country.jsx
👉 Code:
import React from 'react';

const Country = ({country}) => {
  console.log(country);
  return (
    <div>
      <h3>Name: {country.name.common}</h3>
    </div>
  );
};
export default Country;

📌 Explanation:
- Country component → প্রতিটি country এর নাম দেখাচ্ছে।
- country.name.common → API data থেকে country এর common name।

4. Output (Example)
Traveling Countries: 250  
Name: Bangladesh  
Name: India  
Name: Nepal  
Name: USA  
... ইত্যাদি

5. Advantages
- Suspense → fallback UI দিয়ে user experience ভালো হয়।
- use() → promise resolve করে সহজে data ব্যবহার করা যায়।
- Component hierarchy → App → Countries → Country → clean এবং modular structure।
- map() → array data সহজে UI তে দেখানো যায়।
- key props → performance এবং warning-free rendering।

📌 Summary
- App.jsx → Suspense দিয়ে Countries component render করছে।
- Countries.jsx → use() দিয়ে countriesPromise resolve করে data দেখাচ্ছে।
- Country.jsx → প্রতিটি country এর নাম দেখাচ্ছে।
- React workflow → promise → Suspense fallback → use() → map() → child component।
- Output → countries এর সংখ্যা এবং নাম list আকারে দেখানো হচ্ছে।



===============================
40-3 Show Flags, Conditional Text and Handle Data Error
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ কিভাবে country flags দেখানো যায়?
- Conditional rendering কীভাবে কাজ করে?
- Data error handle করার উপায় কী?
- Component hierarchy কেমন হবে?
- প্রতিটি code অংশ কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. App.jsx
👉 Code:
import { Suspense } from "react";
import "./App.css";
import Countries from "./components/Countries/Countries";

const countriesPromise = fetch("countries.json").then(res => res.json());

function App() {
  return (
    <>
      <Suspense fallback={<h1>Loading..........</h1>}>
        <Countries countriesPromise={countriesPromise}></Countries>
      </Suspense>
    </>
  );
}
export default App;

📌 Explanation:
- countriesPromise → fetch() দিয়ে local JSON file থেকে countries data আনা হচ্ছে।
- Suspense → asynchronous data load হওয়ার সময় fallback UI দেখায়।
- Countries component → countriesPromise props হিসেবে পাচ্ছে।

---

2. Countries.jsx
👉 Code:
import React, { use } from 'react';
import Country from '../Country/Country';

const Countries = ({countriesPromise}) => {
  const countries = use(countriesPromise);
  console.log(countries);
  return (
    <div>
      <h1>Traveling Countries: {countries.length}</h1>
      {countries.map(country => <Country key={country.cca3} country={country}></Country>)}
    </div>
  );
};
export default Countries;

📌 Explanation:
- use(countriesPromise) → promise resolve করে countries data return করছে।
- countries.length → মোট কতগুলো country আছে তা দেখাচ্ছে।
- countries.map() → প্রতিটি country কে Country component এ পাঠাচ্ছে।
- key={country.cca3} → প্রতিটি list item এর unique identity (best practice)।

---

3. Country.jsx
👉 Code:
import React from 'react';

const Country = ({country}) => {
  console.log(country);
  return (
    <div>
      <h3>Name: {country.name.common}</h3>
      <img src={country.flags.png} alt="" />
      <p>Independent: {country.independent ? 'Free' : 'Not Free'}</p>
      <p>Population: {country.population}</p>
    </div>
  );
};
export default Country;

📌 Explanation:
- country.name.common → দেশের নাম দেখাচ্ছে।
- <img src={country.flags.png} /> → দেশের flag দেখাচ্ছে।
- Conditional rendering → country.independent true হলে "Free", না হলে "Not Free"।
- country.population → দেশের population দেখাচ্ছে।

---

4. Output (Example)
Traveling Countries: 250  
Name: Bangladesh  
[Flag image]  
Independent: Free  
Population: 170000000  

Name: India  
[Flag image]  
Independent: Free  
Population: 1400000000  

---

5. Error Handling (Concept)
- Data fetch error হলে fallback UI বা error message দেখানো উচিত।
👉 Example:
fetch("countries.json")
  .then(res => res.json())
  .catch(error => console.error("Error loading countries:", error));

📌 Explanation:
- .catch() → error হলে console এ দেখাবে বা UI তে message render করা যাবে।
- Suspense fallback → loading state handle করে।

---

6. Advantages
- Flag image → UI visually সুন্দর হয়।
- Conditional rendering → স্বাধীন দেশ আর অস্বাধীন দেশ আলাদা করে দেখানো যায়।
- Population data → তথ্যপূর্ণ UI।
- Suspense fallback → loading state handle করা যায়।
- Error handling → API বা data সমস্যা হলে user-friendly message দেওয়া যায়।

---

📌 Summary
- App.jsx → Suspense দিয়ে Countries component render করছে।
- Countries.jsx → use() দিয়ে countriesPromise resolve করে data দেখাচ্ছে।
- Country.jsx → দেশের নাম, flag, স্বাধীনতা status, population দেখাচ্ছে।
- Conditional rendering → স্বাধীন হলে "Free", না হলে "Not Free"।
- Error handling → .catch() দিয়ে data error manage করা যায়।
- Output → দেশের সংখ্যা, নাম, flag, স্বাধীনতা status, population list আকারে দেখানো হচ্ছে।


===============================
40-4 Use Style in React and 3 Column Layout
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ কিভাবে CSS style ব্যবহার করা যায়?
- Grid layout দিয়ে ৩ column structure কিভাবে তৈরি হয়?
- Component এ className দিয়ে style apply করার নিয়ম কী?
- Code এর প্রতিটি অংশ কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Countries.jsx
👉 Code:
import React, { use } from 'react';
import Country from '../Country/Country';
import './Countries.css'

const Countries = ({countriesPromise}) => {
  const countries = use(countriesPromise);
  console.log(countries);
  return (
    <div>
      <h1>Traveling Countries: {countries.length}</h1>
      <div className='countries'>
        {countries.map(country => <Country key={country.cca3} country={country}></Country>)}
      </div>
    </div>
  );
};
export default Countries;

📌 Explanation:
- countriesPromise → API থেকে আসা promise resolve করে countries data পাওয়া যাচ্ছে।
- countries.length → মোট কতগুলো country আছে তা দেখাচ্ছে।
- countries.map() → প্রতিটি country কে Country component এ পাঠাচ্ছে।
- className='countries' → CSS grid layout apply করা হচ্ছে।

---

2. Countries.css
👉 Code:
.countries {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20;
}

📌 Explanation:
- display: grid → container কে grid layout বানাচ্ছে।
- grid-template-columns: repeat(3, 1fr) → ৩টা column তৈরি হচ্ছে, প্রতিটি সমান জায়গা নিচ্ছে।
- gap: 20 → column এবং row এর মধ্যে space দিচ্ছে (এখানে px unit দেওয়া উচিত → gap: 20px)।

---

3. Country.jsx
👉 Code:
import React from 'react';
import './Country.css';

const Country = ({country}) => {
  const handleVisited = () => {
    console.log('visited clicked');
  }
  return (
    <div className='country'>
      <h3>Name: {country.name.common}</h3>
      <img src={country.flags.png} alt="" />
      <p>Independent: {country.independent ? 'Free' : 'Not Free'}</p>
      <p>Population: {country.population}</p>
      <button onClick={handleVisited}>Not Visited</button>
    </div>
  );
};
export default Country;

📌 Explanation:
- country.name.common → দেশের নাম দেখাচ্ছে।
- country.flags.png → দেশের flag image দেখাচ্ছে।
- Conditional rendering → independent হলে "Free", না হলে "Not Free"।
- country.population → দেশের population দেখাচ্ছে।
- Button → click করলে handleVisited function console এ log করবে।
- className='country' → CSS style apply হচ্ছে।

---

4. Country.css
👉 Code:
.country {
  border: 2px solid tomato;
  padding: 10px;
  border-radius: 15px;
}

📌 Explanation:
- border → প্রতিটি country card এর চারপাশে tomato color এর border।
- padding → ভিতরের content এর চারপাশে space।
- border-radius → card এর corner গুলো গোলাকার করছে।

---

5. Output (Example)
Traveling Countries: 250  

[৩ column layout এ countries দেখাবে]  

Card Example:  
Name: Bangladesh  
[Flag image]  
Independent: Free  
Population: 170000000  
[Button: Not Visited]  

---

6. Advantages
- CSS grid → responsive এবং সুন্দর layout তৈরি করে।
- className → component-specific style apply করা যায়।
- Reusable component → প্রতিটি country আলাদা card আকারে render হয়।
- Button → future এ visited state track করার জন্য ব্যবহার করা যাবে।

---

📌 Summary
- Countries.jsx → countries data map করে Country component এ পাঠাচ্ছে।
- Countries.css → grid layout তৈরি করছে, ৩ column structure।
- Country.jsx → দেশের নাম, flag, স্বাধীনতা status, population এবং button দেখাচ্ছে।
- Country.css → প্রতিটি card এর জন্য border, padding, rounded corner style দিচ্ছে।
- Output → countries সুন্দরভাবে ৩ column layout এ card আকারে দেখানো হচ্ছে।



===============================
40-5 Conditional CSS and Toggle State Based on Click
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ conditional CSS কিভাবে ব্যবহার করা যায়?
- useState দিয়ে toggle state কিভাবে কাজ করে?
- Button click করলে UI কিভাবে পরিবর্তন হয়?
- Code এর প্রতিটি অংশ কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. useState Hook
👉 Code:
const [visited, setVisited] = useState(false);

📌 Explanation:
- visited → state variable (default মান false)।
- setVisited → state update করার function।
- useState(false) → প্রথমে country visited নয়।

---

2. Toggle Function
👉 Code:
const handleVisited = () => {
  setVisited(!visited);
}

📌 Explanation:
- handleVisited → button click করলে call হয়।
- setVisited(!visited) → visited state কে উল্টে দেয় (true হলে false, false হলে true)।
- এর ফলে প্রতিবার click করলে visited state toggle হয়।

---

3. Conditional CSS in JSX
👉 Code:
<div className={`country ${visited && 'country-visited'}`}>

📌 Explanation:
- className → dynamic ভাবে CSS class apply হচ্ছে।
- যদি visited true হয় → "country country-visited" class apply হবে।
- যদি visited false হয় → শুধু "country" class থাকবে।

---

4. Conditional Button Styling
👉 Code:
<button className={visited ? 'btn-visited' : 'btn-not-visited'} onClick={handleVisited}>
  {visited ? 'Visited' : 'Not Visited'}
</button>

📌 Explanation:
- Button এর className → visited true হলে 'btn-visited', না হলে 'btn-not-visited'।
- Button এর text → visited true হলে "Visited", না হলে "Not Visited"।
- onClick → handleVisited function call করে state toggle করে।

---

5. CSS Styling
👉 Code:
.country-visited {
  background-color: darkgrey;
}

📌 Explanation:
- country-visited class → background color darkgrey করে।
- visited হলে card এর background পরিবর্তন হবে।

---

6. Output (Example)
Card Example:  
Name: Bangladesh  
[Flag image]  
Independent: Free  
Population: 170000000  
[Button: Not Visited]  

👉 Button click করলে:  
- Background → darkgrey হবে।  
- Button text → "Visited" হবে।  
- Button class → btn-visited হবে।  

---

7. Advantages
- Conditional CSS → UI dynamic হয়।
- Toggle state → user interaction অনুযায়ী UI পরিবর্তন হয়।
- Reusable component → প্রতিটি country card এ একই logic কাজ করে।
- Simple এবং clean code → state + conditional rendering দিয়ে কাজ সম্পন্ন।

---

📌 Summary
- useState → visited state manage করছে।
- handleVisited → state toggle করছে।
- Conditional CSS → visited হলে background পরিবর্তন হচ্ছে।
- Button → text এবং style state অনুযায়ী পরিবর্তন হচ্ছে।
- Output → প্রতিটি country card এ visited toggle করা যায়।



===============================
40-6 Lift Up the State to Handle Child Component Event
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Lift up the state মানে কী?
- কেন parent component এ state রাখা হয়?
- Child component থেকে parent এ event কিভাবে পাঠানো যায়?
- Code এর প্রতিটি অংশ কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept: Lift Up the State
- React এ কখনো state child component এ না রেখে parent এ রাখা হয়।
- কারণ → একাধিক child component কে একই state share করতে হয়।
- Child থেকে parent এ data পাঠানোর জন্য function props হিসেবে পাঠানো হয়।
👉 Example:
Parent → state রাখে  
Child → event trigger করে  
Parent → state update করে  

---

2. Countries.jsx (Parent Component)
👉 Code:
import React, { use, useState } from 'react';
import Country from '../Country/Country';
import './Countries.css'

const Countries = ({countriesPromise}) => {
  const [visitedCountries, setVisitedCountries] = useState([]);

  const countries = use(countriesPromise);

  const handleVisitedCountries = (country) => {
    console.log('country visited clicked to be added', country);
    // এখানে future এ setVisitedCountries([...visitedCountries, country]) ব্যবহার করা যাবে
  }

  return (
    <div>
      <h1>Traveling Countries: {countries.length}</h1>
      <h3>Traveled so far: </h3>
      <div className='countries'>
        {countries.map(country => 
          <Country 
            key={country.cca3} 
            handleVisitedCountries={handleVisitedCountries}
            country={country}></Country>
        )}
      </div>
    </div>
  );
};
export default Countries;

📌 Explanation:
- visitedCountries → parent এ state রাখা হয়েছে।
- handleVisitedCountries → child থেকে call হবে, country data parent এ আসবে।
- countries.map() → প্রতিটি country কে Country component এ পাঠানো হচ্ছে।
- handleVisitedCountries props হিসেবে child এ পাঠানো হয়েছে।

---

3. Country.jsx (Child Component)
👉 Code:
import React, { useState } from 'react';
import './Country.css';

const Country = ({country, handleVisitedCountries}) => {
  const [visited, setVisited] = useState(false);

  const handleVisited = () => {
    setVisited(!visited);
    handleVisitedCountries(country);
  }

  return (
    <div className={`country ${visited && 'country-visited'}`}>
      <h3>Name: {country.name.common}</h3>
      <img src={country.flags.png} alt="" />
      <p>Independent: {country.independent ? 'Free' : 'Not Free'}</p>
      <p>Population: {country.population}</p>
      <button 
        className={visited ? 'btn-visited' : 'btn-not-visited'} 
        onClick={handleVisited}>
        {visited ? 'Visited' : 'Not Visited'}
      </button>
    </div>
  );
};
export default Country;

📌 Explanation:
- visited → child component এর local state (true/false)।
- handleVisited → button click করলে visited toggle হয়।
- handleVisitedCountries(country) → parent এ country data পাঠানো হয়।
- Conditional CSS → visited হলে background পরিবর্তন হয়।
- Button → text এবং style state অনুযায়ী পরিবর্তন হয়।

---

4. Flow of Data
- Parent (Countries.jsx) → state রাখে (visitedCountries)।
- Child (Country.jsx) → button click করলে parent এর function call করে।
- Parent → সেই country কে visitedCountries state এ যোগ করতে পারে।
- এভাবে parent সব visited countries track করতে পারে।

---

5. Output (Example)
Traveling Countries: 250  
Traveled so far:  

Card Example:  
Name: Bangladesh  
[Flag image]  
Independent: Free  
Population: 170000000  
[Button: Not Visited → click করলে "Visited"]  

👉 Button click করলে:
- Card background পরিবর্তন হবে।
- Button text "Visited" হবে।
- Parent console এ country log করবে।

---

6. Advantages
- Lift up the state → parent component সব child এর data track করতে পারে।
- Child → শুধু event trigger করে, state parent এ যায়।
- Reusable এবং scalable design → বড় project এ সহজে manage করা যায়।
- Data flow → predictable এবং সহজে বোঝা যায়।

---

📌 Summary
- Lift up the state → parent এ state রাখা হয় যাতে একাধিক child share করতে পারে।
- Countries.jsx → parent state visitedCountries রাখছে এবং function child এ পাঠাচ্ছে।
- Country.jsx → button click করলে parent এর function call করছে।
- Conditional CSS → visited হলে UI পরিবর্তন হচ্ছে।
- Output → প্রতিটি country card এ visited toggle করা যায় এবং parent এ data পাঠানো যায়।



===============================
40-7 (Advanced) Lift Up State in Arrays by Comparing Reference
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Lift up state মানে কী?
- কেন parent component এ state রাখা হয়?
- Array state update করার সময় reference comparison কেন দরকার?
- Code এর প্রতিটি অংশ কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept: Lift Up the State
- React এ state parent এ রাখা হয় যাতে একাধিক child একই state share করতে পারে।
- Child component শুধু event trigger করে, parent state update করে।
- Parent state → সব child এর data track করতে পারে।
👉 Example: Countries.jsx parent এ visitedCountries রাখা হয়েছে, Country.jsx child থেকে parent এ data পাঠাচ্ছে।

---

2. Countries.jsx (Parent Component)
👉 Code:
import React, { use, useState } from 'react';
import Country from '../Country/Country';
import './Countries.css'

const Countries = ({countriesPromise}) => {
  const [visitedCountries, setVisitedCountries] = useState([]);
  const countries = use(countriesPromise);

  const handleVisitedCountries = (country) => {
    console.log('country visited clicked to be added', country);
    const newVisitedCountries = [...visitedCountries, country];
    setVisitedCountries(newVisitedCountries);
  }

  return (
    <div>
      <h1>Traveling Countries: {countries.length}</h1>
      <h3>Traveled so far: {visitedCountries.length}</h3>
      <ol>
        {visitedCountries.map(country => <li key={country.cca3}>{country.name.common}</li>)}
      </ol>
      <div className='countries'>
        {countries.map(country => 
          <Country 
            key={country.cca3} 
            handleVisitedCountries={handleVisitedCountries}
            country={country}></Country>
        )}
      </div>
    </div>
  );
};
export default Countries;

📌 Explanation:
- visitedCountries → parent state, array আকারে visited countries track করছে।
- handleVisitedCountries(country) → child থেকে call হলে নতুন country array তে যোগ হচ্ছে।
- [...visitedCountries, country] → নতুন array তৈরি হচ্ছে (immutability বজায় রাখতে)।
- setVisitedCountries(newVisitedCountries) → React re-render trigger করে।
- visitedCountries.map() → visited countries list আকারে দেখাচ্ছে।
- countries.map() → সব countries কে Country component এ পাঠাচ্ছে।

---

3. Advanced Concept: Comparing Reference
- React state update করার সময় reference change হওয়া জরুরি।
- visitedCountries.push(country) ব্যবহার করলে পুরনো array mutate হয় → React re-render করে না।
- [...visitedCountries, country] → নতুন array তৈরি হয় → React বুঝতে পারে state পরিবর্তন হয়েছে।
- Reference change → React re-render trigger করে এবং UI update হয়।

---

4. Output (Example)
Traveling Countries: 250  
Traveled so far: 2  
- Bangladesh  
- India  

👉 যখন কোনো country তে "Visited" button চাপা হয়:
- সেই country parent এর visitedCountries array তে যোগ হয়।
- UI তে visited countries list update হয়।
- visitedCountries.length → visited countries এর সংখ্যা দেখায়।

---

5. Advantages
- Lift up state → parent সব visited countries track করতে পারে।
- Array immutability → React re-render সঠিকভাবে কাজ করে।
- Child → শুধু event trigger করে, parent state update করে।
- Reusable এবং scalable design → বড় project এ সহজে manage করা যায়।
- Predictable data flow → parent → child → parent communication পরিষ্কার থাকে।

---

📌 Summary
- Lift up state → parent এ state রাখা হয় যাতে একাধিক child share করতে পারে।
- Countries.jsx → visitedCountries state array আকারে manage করছে।
- handleVisitedCountries → child থেকে parent এ country পাঠাচ্ছে।
- Array update → নতুন array তৈরি করে reference change করা হচ্ছে।
- Output → visited countries parent এ list আকারে দেখানো হচ্ছে।


===============================
40-8 (Recap) Lift Up State for Flags and Handle Immutable Array
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Lift up state মানে কী?
- কেন parent component এ state রাখা হয়?
- Immutable array কেন দরকার?
- Child থেকে parent এ event কিভাবে পাঠানো যায়?
- Code এর প্রতিটি অংশ কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Concept: Lift Up the State
- React এ state parent এ রাখা হয় যাতে একাধিক child একই state share করতে পারে।
- Child শুধু event trigger করে, parent state update করে।
- এখানে parent দুই ধরনের state রাখছে:
  1. visitedCountries → কোন কোন দেশ visited হয়েছে।
  2. visitedFlags → visited দেশগুলোর flag image।

---

2. Countries.jsx (Parent Component)
👉 Code:
import React, { use, useState } from 'react';
import Country from '../Country/Country';
import './Countries.css'

const Countries = ({countriesPromise}) => {
  const [visitedCountries, setVisitedCountries] = useState([]);
  const [visitedFlags, setVisitedFlags] = useState([]);
  const countries = use(countriesPromise);

  const handleVisitedFlag = (flag) => {
    const newVisitedFlags = [...visitedFlags, flag];
    setVisitedFlags(newVisitedFlags);
  }

  const handleVisitedCountries = (country) => {
    const newVisitedCountries = [...visitedCountries, country];
    setVisitedCountries(newVisitedCountries);
  }

  return (
    <div>
      <h1>Traveling Countries: {countries.length}</h1>
      <h3>Traveled so far: {visitedCountries.length}</h3>
      <div className='visited-flags-container'>
        {visitedFlags.map((flag, index) => <img key={index} src={flag}></img>)}
      </div>
      <ol>
        {visitedCountries.map(country => <li key={country.cca3}>{country.name.common}</li>)}
      </ol>
      <div className='countries'>
        {countries.map(country => 
          <Country 
            key={country.cca3} 
            handleVisitedCountries={handleVisitedCountries}
            handleVisitedFlag={handleVisitedFlag}
            country={country}></Country>
        )}
      </div>
    </div>
  );
};
export default Countries;

📌 Explanation:
- visitedCountries → parent state, array আকারে visited countries track করছে।
- visitedFlags → parent state, array আকারে visited flags track করছে।
- handleVisitedCountries(country) → child থেকে call হলে নতুন country array তে যোগ হয়।
- handleVisitedFlag(flag) → child থেকে call হলে flag image array তে যোগ হয়।
- [...visitedCountries, country] এবং [...visitedFlags, flag] → নতুন array তৈরি হচ্ছে (immutability বজায় রাখতে)।
- visitedCountries.map() → visited countries list আকারে দেখাচ্ছে।
- visitedFlags.map() → visited flags image আকারে দেখাচ্ছে।

---

3. Country.jsx (Child Component)
👉 Code:
import React, { useState } from 'react';
import './Country.css';

const Country = ({country, handleVisitedCountries, handleVisitedFlag}) => {
  const [visited, setVisited] = useState(false);

  const handleVisited = () => {
    setVisited(!visited);
    handleVisitedCountries(country);
  }

  return (
    <div className={`country ${visited && 'country-visited'}`}>
      <h3>Name: {country.name.common}</h3>
      <img src={country.flags.png} alt="" />
      <p>Independent: {country.independent ? 'Free' : 'Not Free'}</p>
      <p>Population: {country.population}</p>
      <button 
        className={visited ? 'btn-visited' : 'btn-not-visited'} 
        onClick={handleVisited}>
        {visited ? 'Visited' : 'Not Visited'}
      </button>
      <button onClick={() => handleVisitedFlag(country.flags.png)}>Add visited Flag</button>
    </div>
  );
};
export default Country;

📌 Explanation:
- visited → child এর local state (true/false)।
- handleVisited → button click করলে visited toggle হয় এবং parent এ country পাঠায়।
- handleVisitedFlag(country.flags.png) → parent এ flag পাঠায়।
- Conditional CSS → visited হলে background পরিবর্তন হয়।
- Button → text এবং style state অনুযায়ী পরিবর্তন হয়।
- দ্বিতীয় button → flag image parent এ পাঠায়।

---

4. Advanced Concept: Immutable Array
- React state update করার সময় reference change হওয়া জরুরি।
- visitedCountries.push(country) ব্যবহার করলে পুরনো array mutate হয় → React re-render করে না।
- [...visitedCountries, country] → নতুন array তৈরি হয় → React বুঝতে পারে state পরিবর্তন হয়েছে।
- একইভাবে flags এর জন্যও [...visitedFlags, flag] ব্যবহার করা হয়েছে।
- Reference change → React re-render trigger করে এবং UI update হয়।

---

5. Output (Example)
Traveling Countries: 250  
Traveled so far: 2  

Visited Flags:  
[Bangladesh flag] [India flag]  

Visited Countries List:  
1. Bangladesh  
2. India  

Card Example:  
Name: Bangladesh  
[Flag image]  
Independent: Free  
Population: 170000000  
[Button: Not Visited → click করলে "Visited"]  
[Button: Add visited Flag → flag parent এ যোগ হবে]  

---

6. Advantages
- Lift up state → parent সব visited countries এবং flags track করতে পারে।
- Array immutability → React re-render সঠিকভাবে কাজ করে।
- Child → শুধু event trigger করে, parent state update করে।
- Reusable এবং scalable design → বড় project এ সহজে manage করা যায়।
- Multiple arrays → একসাথে countries এবং flags manage করা যায়।

---

📌 Summary
- Lift up state → parent এ state রাখা হয় যাতে একাধিক child share করতে পারে।
- Countries.jsx → visitedCountries এবং visitedFlags state array আকারে manage করছে।
- Country.jsx → button click করলে parent এর function call করছে।
- Array update → নতুন array তৈরি করে reference change করা হচ্ছে।
- Output → visited countries list এবং visited flags parent এ দেখানো হচ্ছে।



===============================
40-9 Deploy Rest Countries to Surge or Netlify
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React (Vite) project deploy করার ধাপ কী?
- Surge এ deploy করার নিয়ম কী?
- Netlify/Vercel এ deploy করার ধাপ কী?
- CNAME file এর কাজ কী?

📝 Notes (বিস্তারিত তথ্য)

1. Vite Build Command
👉 Code:
npm run build

📌 Explanation:
- Vite project deploy করার আগে build করতে হয়।
- build করলে → optimized production-ready files তৈরি হয়।
- Output → dist folder এ সব static files থাকে।

---

2. Deploy to Surge
👉 Steps:
1. Surge install → npm install -g surge  
2. Build project → npm run build  
3. Deploy → surge dist  

📌 Explanation:
- surge dist → dist folder কে Surge server এ upload করে।
- প্রথমবার deploy করলে Surge account তৈরি করতে বলে।
- Public folder এ CNAME file দিলে custom domain সেট করা যায়।

👉 Example:
public > CNAME > hello-hello.surge.sh  
- এই ফাইলের মধ্যে custom domain name লিখে দিলে সেই নাম দিয়ে deploy হবে।

---

3. Deploy to Netlify
👉 Steps:
1. Netlify এ account খুলুন।  
2. Project build করুন → npm run build  
3. Netlify dashboard এ গিয়ে "New Site from Git" নির্বাচন করুন।  
4. GitHub repository connect করুন।  
5. Build command → npm run build  
6. Publish directory → dist  

📌 Explanation:
- Netlify স্বয়ংক্রিয়ভাবে build করে deploy করে।
- dist folder → Netlify এ host হয়।
- Custom domain Netlify settings থেকে configure করা যায়।

---

4. Deploy to Vercel
👉 Steps:
1. Vercel এ account খুলুন।  
2. GitHub repository connect করুন।  
3. Framework → Vite নির্বাচন করুন।  
4. Build command → npm run build  
5. Output directory → dist  

📌 Explanation:
- Vercel স্বয়ংক্রিয়ভাবে build করে deploy করে।
- Custom domain Vercel settings থেকে configure করা যায়।
- Vercel → serverless functions + frontend hosting support করে।

---

5. Code Explanation
👉 npm run build
- Vite project কে production-ready static files এ convert করে।
- dist folder এ optimized HTML, CSS, JS থাকে।

👉 surge dist
- dist folder কে Surge server এ upload করে।
- Custom domain থাকলে CNAME file ব্যবহার হয়।

👉 public > CNAME > hello-hello.surge.sh
- CNAME file → custom domain সেট করে।
- Deploy করার সময় Surge সেই domain ব্যবহার করে।

---

6. Output (Example)
- Surge deploy → https://hello-hello.surge.sh  
- Netlify deploy → https://your-app.netlify.app  
- Vercel deploy → https://your-app.vercel.app  

---

7. Advantages
- Surge → সহজ CLI deploy, দ্রুত কাজ করে।  
- Netlify → GitHub integration, auto-deploy, free SSL।  
- Vercel → serverless functions + frontend hosting, modern workflow।  
- সবগুলো → free tier support করে, custom domain দেওয়া যায়।  

---

📌 Summary
- Vite project deploy করার আগে npm run build করতে হয়।  
- Surge এ deploy → npm install -g surge → npm run build → surge dist।  
- Netlify/Vercel → GitHub connect করে auto-deploy হয়।  
- CNAME file → custom domain সেট করার জন্য ব্যবহার হয়।  
- Output → dist folder host হয় এবং custom domain দিয়ে access করা যায়।  
